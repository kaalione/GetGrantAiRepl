Now build the admin interface for managing scraper sources.

PAGES TO CREATE:

1. app/admin/sources/page.tsx:
   - Table showing all scraper sources from scraper_sources table
   - Columns: Name, Type, URL, Active, Last Scraped, Actions
   - "Add New Source" button
   - Toggle to activate/deactivate sources
   - "Run Now" button to trigger immediate scrape

2. app/admin/sources/new/page.tsx:
   - Form to add new scraper source
   - Fields:
     * Name (text input)
     * Type (select: "api" or "scrape")
     * URL (text input)
     * Scraper Type (select: "playwright", "beautifulsoup", "api") - only if type is "scrape"
     * Update Frequency (select: "daily", "weekly", "monthly")
     
   - For scrape type, show additional fields:
     * Selectors (JSON editor or dynamic fields):
       - title_selector
       - description_selector
       - deadline_selector
       - url_selector
   
   - For API type, show:
     * API Key (text input, encrypted)
     * API Endpoint structure
     * Headers (JSON)

3. app/admin/sources/[id]/edit/page.tsx:
   - Same form as new, but pre-filled with existing data
   - "Delete Source" button with confirmation

4. app/admin/logs/page.tsx:
   - Table showing scraper_logs
   - Filters: date range, status, source
   - Show: timestamp, source name, status, grants found, error messages

API ROUTES TO CREATE:

1. app/api/admin/sources/route.ts:
   - GET: List all sources
   - POST: Create new source

2. app/api/admin/sources/[id]/route.ts:
   - GET: Get single source
   - PUT: Update source
   - DELETE: Delete source

3. app/api/admin/sources/[id]/run/route.ts:
   - POST: Trigger immediate scrape for this source
   - Calls Python scraper service

COMPONENTS TO CREATE:

1. components/admin/SourceTable.tsx:
   - Reusable table with sorting and filtering
   - Status badges (green=active, red=inactive)

2. components/admin/SourceForm.tsx:
   - Reusable form for create/edit
   - Form validation with zod

3. components/admin/JsonEditor.tsx:
   - Simple JSON editor for selectors/config
   - Syntax highlighting

STYLING:
- Use shadcn/ui components: Table, Button, Form, Input, Select, Badge, Card
- Admin layout with sidebar navigation
- Responsive design

Implement these admin features now.