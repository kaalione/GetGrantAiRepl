node_modules/
.venv/
venv/
__pycache__/
.cache/
.local/
.playwright/
.config/
dist/
uploads/
*.pyc
*.bin
# Every .env variant is secret — .env.backup held live keys and was tracked
# only by luck. .env.example is the documented template and stays in git.
.env
.env.*
!.env.example

# macOS metadata (AppleDouble files on external volumes)
._*
**/._*
.DS_Store

# Claude Code local state
.claude/worktrees/
.agents/
private-uploads/
