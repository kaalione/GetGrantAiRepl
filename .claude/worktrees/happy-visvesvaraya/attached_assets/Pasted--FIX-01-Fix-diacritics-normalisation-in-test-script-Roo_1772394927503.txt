
FIX 01  Fix diacritics normalisation in test script
Root cause
The test keyword matching uses raw string comparison. Swedish grant source names contain å, ö, ä which don't match the ASCII keywords in the test company definitions. This causes false failures on Checks A and B for companies like C3 (Norrbotten), C4 (Malmö / Skåne), and C5 (Kulturrådet).
 
Files to change: scripts/test-matching-quality.ts
📋  Replit Prompt
 
═══════════════════════════════════════════════════════
TASK: Fix keyword matching in test script for Swedish characters.
Do NOT change any production code.
═══════════════════════════════════════════════════════
 
In scripts/test-matching-quality.ts, find the keywordInText function
(or wherever source name / title matching is done for Checks A and B).
 
STEP 1 — Add a normalisation helper at the top of the file:
 
function normalizeStr(str: string): string {
 return str
   .normalize('NFD')                    // decompose accented chars
   .replace(/\p{Diacritic}/gu, '')       // strip diacritics
   .toLowerCase()
   .replace(/[^a-z0-9]/g, '');          // strip non-alphanumeric
}
 
STEP 2 — Replace all keyword comparison logic with:
 
// OLD (broken):
// const matches = keyword => sourceName.toLowerCase().includes(keyword)
 
// NEW (fixed):
const matches = (keyword: string, text: string) =>
 normalizeStr(text).includes(normalizeStr(keyword));
 
Apply this to both:
 - Check A: matching expectedInTop10 against grant sourceName and title
 - Check B: matching expectedNotInTop10 against grant sourceName and title
 
STEP 3 — Also update the test company keyword arrays to remove any
manual diacritic workarounds that may have been added. For example:
 'kulturradet'  stays as-is (normalizeStr will handle it)
 'Kulturrådet'  also fine (normalizeStr strips the å)
 
STEP 4 — Run the test:
 npx tsx scripts/test-matching-quality.ts
 
STEP 5 — Record which companies now pass Checks A and B.
 Update the JSON report and log the before/after numbers.
✅  Verification
1. Run: npx tsx scripts/test-matching-quality.ts
2. C3 (NordAqua, Norrbotten) — Check A should now pass (jordbruksverket matches Jordbruksverket)
3. C4 (Medianova, Malmö) — Check A should now pass (skane matches Skåne)
4. C5 (Kulturkompaniet) — Check A should now pass (kulturradet matches Kulturrådet)
5. Overall pass count should rise from 0/10 to at least 4/10 from this fix alone