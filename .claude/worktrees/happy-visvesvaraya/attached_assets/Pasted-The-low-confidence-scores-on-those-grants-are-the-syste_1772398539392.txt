The low confidence scores on those grants are the system working correctly — don’t touch them. Trying to extract from a 120-character title echo is a waste of API budget and will produce garbage that actively hurts matching quality.
Here’s the honest assessment of Regionalforvaltning.no: this is a scraper architecture problem, not an extraction problem. The scraper is capturing list-page data (title + URL) but not following through to the individual grant detail pages where the actual description, eligibility criteria, and funding amounts live. Fixing it properly means adding Playwright-based detail-page fetching to the Norwegian regional scraper — probably a 4–6 hour job. That’s worth doing eventually, but not before beta.
What to do right now (30 minutes total):
Run extraction on the 76 never-extracted EU Funding & Tenders grants:

POST /api/cron/extract-eligibility
Body: { "batchSize": 20, "maxGrants": 80, "onlyMissing": true, "delayMs": 2000 }


Then check the 3 tiny gaps — Naturvårdsverket (2 grants) and Vinnova Funding API (1 grant). Just look at them manually in the DB:

SELECT title, LEFT(description, 300), eligibility_criteria
FROM grants
WHERE source_name IN ('Naturvårdsverket', 'Vinnova Funding API')
  AND (structured_eligibility->>'confidence_score')::float < 0.3;


If the descriptions are substantial, trigger single-grant re-extraction via /api/admin/eligibility/extract/:grantId. If they’re also thin, leave them.
After that, rerun the matching test. At 80.4% coverage with Vinnova at 79% and the core Swedish sources well covered, Check C may already be passing for several companies without doing anything else. That’s the number that actually matters now.
The Regionalforvaltning.no scraper improvement goes on the backlog with a clear brief: add detail-page fetching, re-extract after. But that’s post-beta work. The platform is ready enough to test with real users, and real users will tell you which Norwegian grant categories matter most — which is a better signal than trying to fix all 161 sparse descriptions in the dark.​​​​​​​​​​​​​​​​