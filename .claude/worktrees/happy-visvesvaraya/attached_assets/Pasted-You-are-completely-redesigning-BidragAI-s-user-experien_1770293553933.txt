You are completely redesigning BidragAI's user experience and interface to be modern, 
intuitive, and conversion-optimized. You will implement:

1. Modern, delightful UI with animations and micro-interactions
2. Guided onboarding flow for new users
3. Improved information architecture
4. Priority features: Export, monetization, tracking
5. Better empty states and loading states
6. Mobile-responsive design

CURRENT PROBLEMS TO SOLVE:
- Generic interface with no personality
- No onboarding - users are lost after signup
- No clear call-to-action hierarchy
- Missing polish and delight
- Not optimized for conversion
- Unclear value proposition

DESIGN PRINCIPLES:
✓ Swedish-first (all text in Swedish)
✓ Fast and responsive
✓ Delightful micro-interactions
✓ Clear information hierarchy
✓ Trust-building elements (social proof, testimonials, stats)
✓ Conversion-optimized (clear CTAs, progress tracking)

═══════════════════════════════════════════════════════════════════════════
PART 1: NEW LANDING PAGE (BEFORE LOGIN)
═══════════════════════════════════════════════════════════════════════════

GOAL: Convert visitors to signups with clear value proposition.

Create src/pages/Landing.tsx:
```typescript
import { useState, useEffect } from 'react';
import { ArrowRight, Sparkles, Target, Zap, Clock, CheckCircle } from 'lucide-react';
import { Button } from '@/components/ui/button';

export function Landing() {
  const [statsVisible, setStatsVisible] = useState(false);

  useEffect(() => {
    // Trigger animation on mount
    setTimeout(() => setStatsVisible(true), 300);
  }, []);

  return (
    <div className="min-h-screen bg-gradient-to-b from-blue-50 to-white dark:from-gray-900 dark:to-gray-800">
      {/* Hero Section */}
      <section className="container mx-auto px-4 pt-20 pb-32">
        <div className="max-w-4xl mx-auto text-center">
          {/* Badge */}
          <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 text-sm font-medium mb-6 animate-fade-in">
            <Sparkles className="h-4 w-4" />
            <span>AI-driven bidragsplattform för svenska företag</span>
          </div>

          {/* Hero Headline */}
          <h1 className="text-5xl md:text-6xl font-bold tracking-tight mb-6 animate-fade-in-up">
            Hitta och ansök om bidrag
            <span className="block text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-cyan-600 dark:from-blue-400 dark:to-cyan-400">
              på 5 minuter
            </span>
          </h1>

          {/* Subheadline */}
          <p className="text-xl text-gray-600 dark:text-gray-300 mb-10 max-w-2xl mx-auto animate-fade-in-up animation-delay-100">
            BidragAI använder AI för att hitta relevanta bidrag, analysera dina chanser,
            och skriva ansökningar åt dig. Helt på svenska.
          </p>

          {/* CTA Buttons */}
          <div className="flex flex-col sm:flex-row gap-4 justify-center mb-12 animate-fade-in-up animation-delay-200">
            <Button
              size="lg"
              className="text-lg px-8 py-6 bg-gradient-to-r from-blue-600 to-cyan-600 hover:from-blue-700 hover:to-cyan-700"
              onClick={() => window.location.href = '/auth/login'}
            >
              Kom igång gratis
              <ArrowRight className="ml-2 h-5 w-5" />
            </Button>
            <Button
              size="lg"
              variant="outline"
              className="text-lg px-8 py-6"
              onClick={() => document.getElementById('how-it-works')?.scrollIntoView({ behavior: 'smooth' })}
            >
              Se hur det fungerar
            </Button>
          </div>

          {/* Trust Indicators */}
          <div className="flex flex-wrap justify-center gap-6 text-sm text-gray-600 dark:text-gray-400 animate-fade-in-up animation-delay-300">
            <div className="flex items-center gap-2">
              <CheckCircle className="h-5 w-5 text-green-500" />
              <span>379+ aktiva bidrag</span>
            </div>
            <div className="flex items-center gap-2">
              <CheckCircle className="h-5 w-5 text-green-500" />
              <span>Helt på svenska</span>
            </div>
            <div className="flex items-center gap-2">
              <CheckCircle className="h-5 w-5 text-green-500" />
              <span>Gratis att börja</span>
            </div>
          </div>
        </div>

        {/* Hero Image/Demo */}
        <div className="mt-16 max-w-5xl mx-auto animate-fade-in-up animation-delay-400">
          <div className="rounded-2xl shadow-2xl border border-gray-200 dark:border-gray-700 overflow-hidden bg-white dark:bg-gray-800">
            {/* Screenshot or demo video placeholder */}
            <div className="aspect-video bg-gradient-to-br from-blue-100 to-cyan-100 dark:from-blue-900/20 dark:to-cyan-900/20 flex items-center justify-center">
              <div className="text-center p-8">
                <Sparkles className="h-16 w-16 mx-auto mb-4 text-blue-600" />
                <p className="text-gray-600 dark:text-gray-400">
                  Screenshot eller demo-video kommer här
                </p>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Stats Section */}
      <section className="py-16 bg-blue-600 dark:bg-blue-900">
        <div className="container mx-auto px-4">
          <div className="grid grid-cols-2 md:grid-cols-4 gap-8 text-center text-white">
            {[
              { value: '379+', label: 'Aktiva bidrag' },
              { value: '67', label: 'Öppna just nu' },
              { value: '5 min', label: 'Genomsnittlig tid' },
              { value: '100%', label: 'Gratis att testa' },
            ].map((stat, i) => (
              <div
                key={i}
                className={`transition-all duration-500 ${
                  statsVisible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-4'
                }`}
                style={{ transitionDelay: `${i * 100}ms` }}
              >
                <div className="text-4xl font-bold mb-2">{stat.value}</div>
                <div className="text-blue-100">{stat.label}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* How It Works */}
      <section id="how-it-works" className="py-24">
        <div className="container mx-auto px-4">
          <div className="max-w-3xl mx-auto text-center mb-16">
            <h2 className="text-4xl font-bold mb-4">Så fungerar det</h2>
            <p className="text-xl text-gray-600 dark:text-gray-300">
              Från företagsprofil till färdig ansökan på bara några minuter
            </p>
          </div>

          <div className="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
            {[
              {
                icon: Target,
                step: '1',
                title: 'Skapa din profil',
                description: 'Berätta om ditt företag - bransch, storlek, och vad ni gör. Tar 2 minuter.',
              },
              {
                icon: Sparkles,
                step: '2',
                title: 'AI hittar matchningar',
                description: 'Vår AI analyserar 379+ bidrag och visar de som passar er bäst.',
              },
              {
                icon: Zap,
                step: '3',
                title: 'Generera ansökan',
                description: 'AI skriver en komplett ansökan på svenska baserat på er profil och bidragskraven.',
              },
            ].map((item, i) => (
              <div
                key={i}
                className="relative p-8 rounded-2xl bg-white dark:bg-gray-800 shadow-lg border border-gray-200 dark:border-gray-700 hover:shadow-xl transition-shadow"
              >
                {/* Step Number */}
                <div className="absolute -top-4 -left-4 w-12 h-12 rounded-full bg-gradient-to-br from-blue-600 to-cyan-600 text-white font-bold flex items-center justify-center text-xl shadow-lg">
                  {item.step}
                </div>

                <item.icon className="h-12 w-12 text-blue-600 dark:text-blue-400 mb-4" />
                <h3 className="text-xl font-semibold mb-3">{item.title}</h3>
                <p className="text-gray-600 dark:text-gray-300">{item.description}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Features Grid */}
      <section className="py-24 bg-gray-50 dark:bg-gray-900">
        <div className="container mx-auto px-4">
          <div className="max-w-3xl mx-auto text-center mb-16">
            <h2 className="text-4xl font-bold mb-4">Allt du behöver</h2>
            <p className="text-xl text-gray-600 dark:text-gray-300">
              En komplett plattform för att hitta och ansöka om bidrag
            </p>
          </div>

          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto">
            {[
              {
                title: '379+ Bidragskällor',
                description: 'Vinnova, Tillväxtverket, EU, och fler. Automatiskt uppdaterat dagligen.',
                icon: '🎯',
              },
              {
                title: 'AI-Matchning',
                description: 'Claude AI analyserar er profil och visar exakt vilka bidrag ni kan söka.',
                icon: '🤖',
              },
              {
                title: 'Automatiska Ansökningar',
                description: 'AI skriver komplett ansökan på svenska baserat på bidragskraven.',
                icon: '✍️',
              },
              {
                title: 'Email-Notiser',
                description: 'Få meddelande när nya matchande bidrag publiceras eller deadlines närmar sig.',
                icon: '📧',
              },
              {
                title: 'Spåra Ansökningar',
                description: 'Hantera alla dina ansökningar på ett ställe. Från utkast till beviljat.',
                icon: '📊',
              },
              {
                title: 'Export till Word',
                description: 'Exportera färdiga ansökningar till .docx för att skicka in direkt.',
                icon: '📄',
              },
            ].map((feature, i) => (
              <div
                key={i}
                className="p-6 rounded-xl bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 hover:border-blue-500 dark:hover:border-blue-500 transition-colors"
              >
                <div className="text-4xl mb-3">{feature.icon}</div>
                <h3 className="text-lg font-semibold mb-2">{feature.title}</h3>
                <p className="text-gray-600 dark:text-gray-400 text-sm">
                  {feature.description}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Social Proof / Testimonials */}
      <section className="py-24">
        <div className="container mx-auto px-4">
          <div className="max-w-3xl mx-auto text-center mb-16">
            <h2 className="text-4xl font-bold mb-4">Vad användare säger</h2>
          </div>

          <div className="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
            {[
              {
                quote: 'Vi hittade 5 relevanta bidrag på 10 minuter. Sparade oss veckor av research.',
                author: 'Erik Andersson',
                company: 'Tech Startup AB',
                avatar: '👨‍💼',
              },
              {
                quote: 'AI-ansökningarna är imponerande. Vi har redan beviljats 2 bidrag via plattformen.',
                author: 'Maria Johansson',
                company: 'GreenTech Solutions',
                avatar: '👩‍💼',
              },
              {
                quote: 'Email-notiserna gör att vi aldrig missar en deadline. Ovärderligt för oss.',
                author: 'Jonas Berg',
                company: 'Innovation Labs',
                avatar: '👨‍🔬',
              },
            ].map((testimonial, i) => (
              <div
                key={i}
                className="p-6 rounded-xl bg-white dark:bg-gray-800 shadow-lg border border-gray-200 dark:border-gray-700"
              >
                <div className="text-4xl mb-4">"</div>
                <p className="text-gray-700 dark:text-gray-300 mb-6 italic">
                  {testimonial.quote}
                </p>
                <div className="flex items-center gap-3">
                  <div className="text-3xl">{testimonial.avatar}</div>
                  <div>
                    <div className="font-semibold">{testimonial.author}</div>
                    <div className="text-sm text-gray-500">{testimonial.company}</div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Pricing */}
      <section className="py-24 bg-gray-50 dark:bg-gray-900">
        <div className="container mx-auto px-4">
          <div className="max-w-3xl mx-auto text-center mb-16">
            <h2 className="text-4xl font-bold mb-4">Enkel prissättning</h2>
            <p className="text-xl text-gray-600 dark:text-gray-300">
              Börja gratis, uppgradera när du behöver mer
            </p>
          </div>

          <div className="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
            {/* Free Tier */}
            <div className="p-8 rounded-2xl bg-white dark:bg-gray-800 border-2 border-gray-200 dark:border-gray-700">
              <div className="text-sm font-semibold text-gray-500 mb-2">GRATIS</div>
              <div className="text-4xl font-bold mb-6">
                0 kr<span className="text-lg text-gray-500">/mån</span>
              </div>
              <ul className="space-y-3 mb-8">
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 text-green-500 mt-0.5" />
                  <span>Se alla 379+ bidrag</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 text-green-500 mt-0.5" />
                  <span>Basic matchning</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 text-green-500 mt-0.5" />
                  <span>3 AI-analyser/månad</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 text-green-500 mt-0.5" />
                  <span>1 AI-ansökan/månad</span>
                </li>
              </ul>
              <Button variant="outline" className="w-full">
                Börja gratis
              </Button>
            </div>

            {/* Pro Tier */}
            <div className="p-8 rounded-2xl bg-gradient-to-br from-blue-600 to-cyan-600 text-white border-2 border-blue-500 relative scale-105 shadow-2xl">
              <div className="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-yellow-400 text-yellow-900 text-sm font-bold">
                POPULÄRAST
              </div>
              <div className="text-sm font-semibold mb-2">PRO</div>
              <div className="text-4xl font-bold mb-6">
                299 kr<span className="text-lg opacity-80">/mån</span>
              </div>
              <ul className="space-y-3 mb-8">
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 mt-0.5" />
                  <span>Allt i Free</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 mt-0.5" />
                  <span>100 AI-analyser/månad</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 mt-0.5" />
                  <span>25 AI-ansökningar/månad</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 mt-0.5" />
                  <span>Email-notiser</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 mt-0.5" />
                  <span>Export till Word/PDF</span>
                </li>
              </ul>
              <Button className="w-full bg-white text-blue-600 hover:bg-gray-100">
                Uppgradera till Pro
              </Button>
            </div>

            {/* Enterprise Tier */}
            <div className="p-8 rounded-2xl bg-white dark:bg-gray-800 border-2 border-gray-200 dark:border-gray-700">
              <div className="text-sm font-semibold text-gray-500 mb-2">ENTERPRISE</div>
              <div className="text-4xl font-bold mb-6">
                2 999 kr<span className="text-lg text-gray-500">/mån</span>
              </div>
              <ul className="space-y-3 mb-8">
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 text-green-500 mt-0.5" />
                  <span>Allt i Pro</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 text-green-500 mt-0.5" />
                  <span>Obegränsad AI-användning</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 text-green-500 mt-0.5" />
                  <span>Team samarbete (5 användare)</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 text-green-500 mt-0.5" />
                  <span>Prioriterad support</span>
                </li>
                <li className="flex items-start gap-2">
                  <CheckCircle className="h-5 w-5 text-green-500 mt-0.5" />
                  <span>API-access</span>
                </li>
              </ul>
              <Button variant="outline" className="w-full">
                Kontakta oss
              </Button>
            </div>
          </div>
        </div>
      </section>

      {/* Final CTA */}
      <section className="py-24 bg-gradient-to-br from-blue-600 to-cyan-600 text-white">
        <div className="container mx-auto px-4 text-center">
          <h2 className="text-4xl md:text-5xl font-bold mb-6">
            Redo att hitta ditt nästa bidrag?
          </h2>
          <p className="text-xl mb-10 opacity-90 max-w-2xl mx-auto">
            Gå med hundratals svenska företag som redan använder BidragAI
          </p>
          <Button
            size="lg"
            className="text-lg px-12 py-6 bg-white text-blue-600 hover:bg-gray-100"
            onClick={() => window.location.href = '/auth/login'}
          >
            Kom igång gratis idag
            <ArrowRight className="ml-2 h-5 w-5" />
          </Button>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-12 bg-gray-900 text-gray-400">
        <div className="container mx-auto px-4">
          <div className="grid md:grid-cols-4 gap-8 mb-8">
            <div>
              <div className="text-white font-bold text-xl mb-4">BidragAI</div>
              <p className="text-sm">
                AI-driven bidragsplattform för svenska företag
              </p>
            </div>
            <div>
              <div className="text-white font-semibold mb-4">Produkt</div>
              <ul className="space-y-2 text-sm">
                <li><a href="#features" className="hover:text-white">Funktioner</a></li>
                <li><a href="#pricing" className="hover:text-white">Priser</a></li>
                <li><a href="#" className="hover:text-white">Dokumentation</a></li>
              </ul>
            </div>
            <div>
              <div className="text-white font-semibold mb-4">Företag</div>
              <ul className="space-y-2 text-sm">
                <li><a href="#" className="hover:text-white">Om oss</a></li>
                <li><a href="#" className="hover:text-white">Kontakt</a></li>
                <li><a href="#" className="hover:text-white">Blogg</a></li>
              </ul>
            </div>
            <div>
              <div className="text-white font-semibold mb-4">Juridiskt</div>
              <ul className="space-y-2 text-sm">
                <li><a href="#" className="hover:text-white">Integritetspolicy</a></li>
                <li><a href="#" className="hover:text-white">Användarvillkor</a></li>
              </ul>
            </div>
          </div>
          <div className="border-t border-gray-800 pt-8 text-center text-sm">
            © 2026 BidragAI. Alla rättigheter förbehållna.
          </div>
        </div>
      </footer>
    </div>
  );
}
```

Add animations to your global CSS:
```css
/* In your global.css or tailwind config */
@keyframes fade-in {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes fade-in-up {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in {
  animation: fade-in 0.6s ease-out;
}

.animate-fade-in-up {
  animation: fade-in-up 0.6s ease-out;
}

.animation-delay-100 {
  animation-delay: 100ms;
}

.animation-delay-200 {
  animation-delay: 200ms;
}

.animation-delay-300 {
  animation-delay: 300ms;
}

.animation-delay-400 {
  animation-delay: 400ms;
}
```

═══════════════════════════════════════════════════════════════════════════
PART 2: ONBOARDING FLOW (AFTER SIGNUP)
═══════════════════════════════════════════════════════════════════════════

GOAL: Guide new users through setup in 3 simple steps.

Create src/components/onboarding/OnboardingWizard.tsx:
```typescript
import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Card } from '@/components/ui/card';
import { Progress } from '@/components/ui/progress';
import { CheckCircle, Building, Sparkles, Rocket } from 'lucide-react';

interface OnboardingStep {
  title: string;
  description: string;
  icon: React.ComponentType<any>;
  component: React.ComponentType<any>;
}

export function OnboardingWizard({ onComplete }: { onComplete: () => void }) {
  const [currentStep, setCurrentStep] = useState(0);
  const [completedSteps, setCompletedSteps] = useState<number[]>([]);

  const steps: OnboardingStep[] = [
    {
      title: 'Välkommen till BidragAI!',
      description: 'Låt oss komma igång på 2 minuter',
      icon: Rocket,
      component: WelcomeStep,
    },
    {
      title: 'Skapa din företagsprofil',
      description: 'Berätta om ditt företag så vi kan hitta rätt bidrag',
      icon: Building,
      component: CompanyProfileStep,
    },
    {
      title: 'Se dina första matchningar',
      description: 'Vi har analyserat 379+ bidrag åt dig',
      icon: Sparkles,
      component: FirstMatchesStep,
    },
  ];

  const progress = ((currentStep + 1) / steps.length) * 100;

  function handleNext(data?: any) {
    setCompletedSteps([...completedSteps, currentStep]);
    
    if (currentStep === steps.length - 1) {
      onComplete();
    } else {
      setCurrentStep(currentStep + 1);
    }
  }

  function handleBack() {
    if (currentStep > 0) {
      setCurrentStep(currentStep - 1);
    }
  }

  const CurrentStepComponent = steps[currentStep].component;
  const StepIcon = steps[currentStep].icon;

  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-cyan-50 dark:from-gray-900 dark:to-gray-800 flex items-center justify-center p-4">
      <Card className="w-full max-w-3xl p-8">
        {/* Progress */}
        <div className="mb-8">
          <div className="flex justify-between mb-2">
            <span className="text-sm font-medium text-gray-600 dark:text-gray-400">
              Steg {currentStep + 1} av {steps.length}
            </span>
            <span className="text-sm font-medium text-gray-600 dark:text-gray-400">
              {Math.round(progress)}%
            </span>
          </div>
          <Progress value={progress} className="h-2" />
        </div>

        {/* Step Header */}
        <div className="text-center mb-8">
          <div className="inline-flex items-center justify-center w-16 h-16 rounded-full bg-blue-100 dark:bg-blue-900/30 mb-4">
            <StepIcon className="h-8 w-8 text-blue-600 dark:text-blue-400" />
          </div>
          <h2 className="text-3xl font-bold mb-2">{steps[currentStep].title}</h2>
          <p className="text-gray-600 dark:text-gray-400">
            {steps[currentStep].description}
          </p>
        </div>

        {/* Step Content */}
        <div className="mb-8">
          <CurrentStepComponent onNext={handleNext} onBack={handleBack} />
        </div>

        {/* Navigation */}
        <div className="flex justify-between items-center">
          <Button
            variant="ghost"
            onClick={handleBack}
            disabled={currentStep === 0}
          >
            Tillbaka
          </Button>

          <div className="flex gap-2">
            {steps.map((_, i) => (
              <div
                key={i}
                className={`w-2 h-2 rounded-full ${
                  i === currentStep
                    ? 'bg-blue-600'
                    : completedSteps.includes(i)
                    ? 'bg-green-500'
                    : 'bg-gray-300'
                }`}
              />
            ))}
          </div>

          <div className="w-24" /> {/* Spacer for symmetry */}
        </div>
      </Card>
    </div>
  );
}

// Step 1: Welcome
function WelcomeStep({ onNext }: { onNext: () => void }) {
  return (
    <div className="space-y-6">
      <div className="aspect-video bg-gradient-to-br from-blue-100 to-cyan-100 dark:from-blue-900/20 dark:to-cyan-900/20 rounded-xl flex items-center justify-center">
        <div className="text-center p-8">
          <Sparkles className="h-16 w-16 mx-auto mb-4 text-blue-600" />
          <p className="text-gray-600 dark:text-gray-400">
            30 sekunders intro-video kommer här
          </p>
          <p className="text-sm text-gray-500 mt-2">
            Visar hur plattformen fungerar
          </p>
        </div>
      </div>

      <div className="grid md:grid-cols-3 gap-4">
        {[
          { icon: '🎯', title: 'Hitta bidrag', text: '379+ källor' },
          { icon: '🤖', title: 'AI-matchning', text: 'Automatisk analys' },
          { icon: '✍️', title: 'Generera ansökningar', text: 'På 5 minuter' },
        ].map((feature, i) => (
          <div key={i} className="text-center p-4 rounded-lg bg-gray-50 dark:bg-gray-800">
            <div className="text-3xl mb-2">{feature.icon}</div>
            <div className="font-semibold mb-1">{feature.title}</div>
            <div className="text-sm text-gray-600 dark:text-gray-400">{feature.text}</div>
          </div>
        ))}
      </div>

      <Button onClick={onNext} size="lg" className="w-full">
        Kom igång
        <ArrowRight className="ml-2 h-5 w-5" />
      </Button>
    </div>
  );
}

// Step 2: Company Profile (simplified form)
function CompanyProfileStep({ onNext }: { onNext: (data: any) => void }) {
  const [formData, setFormData] = useState({
    companyName: '',
    industry: '',
    employees: '',
    description: '',
  });

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    
    // Save to backend
    try {
      const res = await fetch('/api/companies', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(formData),
      });
      
      if (res.ok) {
        const company = await res.json();
        onNext(company);
      }
    } catch (error) {
      console.error('Failed to create company:', error);
    }
  }

  return (
    <form onSubmit={handleSubmit} className="space-y-6">
      <div>
        <label className="block text-sm font-medium mb-2">
          Företagsnamn *
        </label>
        <input
          type="text"
          required
          className="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-600 focus:ring-2 focus:ring-blue-500"
          placeholder="Mitt Företag AB"
          value={formData.companyName}
          onChange={(e) => setFormData({ ...formData, companyName: e.target.value })}
        />
      </div>

      <div>
        <label className="block text-sm font-medium mb-2">
          Bransch *
        </label>
        <select
          required
          className="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-600 focus:ring-2 focus:ring-blue-500"
          value={formData.industry}
          onChange={(e) => setFormData({ ...formData, industry: e.target.value })}
        >
          <option value="">Välj bransch</option>
          <option value="tech">Teknik/IT</option>
          <option value="manufacturing">Tillverkning</option>
          <option value="services">Tjänster</option>
          <option value="retail">Handel</option>
          <option value="other">Annat</option>
        </select>
      </div>

      <div>
        <label className="block text-sm font-medium mb-2">
          Antal anställda *
        </label>
        <select
          required
          className="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-600 focus:ring-2 focus:ring-blue-500"
          value={formData.employees}
          onChange={(e) => setFormData({ ...formData, employees: e.target.value })}
        >
          <option value="">Välj storlek</option>
          <option value="1-10">1-10</option>
          <option value="11-50">11-50</option>
          <option value="51-250">51-250</option>
          <option value="250+">250+</option>
        </select>
      </div>

      <div>
        <label className="block text-sm font-medium mb-2">
          Beskriv er verksamhet (kort) *
        </label>
        <textarea
          required
          rows={4}
          className="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-600 focus:ring-2 focus:ring-blue-500"
          placeholder="Vi utvecklar AI-lösningar för..."
          value={formData.description}
          onChange={(e) => setFormData({ ...formData, description: e.target.value })}
        />
        <p className="text-sm text-gray-500 mt-1">
          Detta hjälper AI:n att hitta rätt bidrag åt er
        </p>
      </div>

      <Button type="submit" size="lg" className="w-full">
        Skapa profil och fortsätt
        <ArrowRight className="ml-2 h-5 w-5" />
      </Button>
    </form>
  );
}

// Step 3: First Matches
function FirstMatchesStep({ onNext }: { onNext: () => void }) {
  const [matches, setMatches] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    // Fetch top matches for user's company
    fetch('/api/grants/top-matches')
      .then(res => res.json())
      .then(data => {
        setMatches(data.slice(0, 3));
        setLoading(false);
      });
  }, []);

  if (loading) {
    return (
      <div className="text-center py-12">
        <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 mx-auto mb-4"></div>
        <p className="text-gray-600">Analyserar 379+ bidrag åt dig...</p>
      </div>
    );
  }

  return (
    <div className="space-y-6">
      <div className="text-center p-6 rounded-xl bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800">
        <CheckCircle className="h-12 w-12 text-green-600 mx-auto mb-3" />
        <p className="text-lg font-semibold text-green-900 dark:text-green-100 mb-1">
          Grattis! Vi hittade {matches.length} matchningar
        </p>
        <p className="text-sm text-green-700 dark:text-green-300">
          Här är bidragen som passar er bäst just nu
        </p>
      </div>

      <div className="space-y-4">
        {matches.map((grant: any) => (
          <div
            key={grant.id}
            className="p-4 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-blue-500 transition-colors"
          >
            <div className="flex items-start justify-between mb-2">
              <h3 className="font-semibold">{grant.title}</h3>
              <div className="flex items-center gap-2">
                <div className="text-2xl font-bold text-blue-600">
                  {grant.matchScore}%
                </div>
              </div>
            </div>
            <p className="text-sm text-gray-600 dark:text-gray-400 mb-3">
              {grant.description?.substring(0, 120)}...
            </p>
            <div className="flex gap-2">
              <span className="text-xs px-2 py-1 rounded-full bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300">
                {grant.sourceName}
              </span>
              {grant.deadline && (
                <span className="text-xs px-2 py-1 rounded-full bg-amber-100 dark:bg-amber-900 text-amber-700 dark:text-amber-300">
                  Deadline: {new Date(grant.deadline).toLocaleDateString('sv-SE')}
                </span>
              )}
            </div>
          </div>
        ))}
      </div>

      <Button onClick={onNext} size="lg" className="w-full">
        Utforska alla bidrag
        <Sparkles className="ml-2 h-5 w-5" />
      </Button>
    </div>
  );
}
```

Show onboarding wizard for new users:
```typescript
// In your main App component or dashboard
export function App() {
  const [showOnboarding, setShowOnboarding] = useState(false);

  useEffect(() => {
    // Check if user is new (has no companies)
    fetch('/api/user/status')
      .then(res => res.json())
      .then(data => {
        if (data.isNewUser && !data.hasCompany) {
          setShowOnboarding(true);
        }
      });
  }, []);

  if (showOnboarding) {
    return (
      <OnboardingWizard
        onComplete={() => {
          setShowOnboarding(false);
          window.location.href = '/dashboard';
        }}
      />
    );
  }

  return <MainApp />;
}
```

═══════════════════════════════════════════════════════════════════════════
PART 3: IMPROVED DASHBOARD (ÖVERSIKT)
═══════════════════════════════════════════════════════════════════════════

Replace your current dashboard with this modern version:
```typescript
import { useEffect, useState } from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Progress } from '@/components/ui/progress';
import {
  TrendingUp,
  Clock,
  CheckCircle,
  AlertCircle,
  Sparkles,
  ArrowRight,
} from 'lucide-react';

export function Dashboard() {
  const [stats, setStats] = useState({
    totalGrants: 0,
    openGrants: 0,
    upcomingDeadlines: 0,
    applications: 0,
    newThisWeek: 0,
    topMatches: [],
  });

  const [completionProgress, setCompletionProgress] = useState(0);

  useEffect(() => {
    // Fetch dashboard data
    fetch('/api/dashboard/stats')
      .then(res => res.json())
      .then(setStats);

    // Check profile completion
    fetch('/api/user/profile-completion')
      .then(res => res.json())
      .then(data => setCompletionProgress(data.percentage));
  }, []);

  return (
    <div className="container mx-auto p-6 space-y-8">
      {/* Hero Section */}
      <div className="relative rounded-2xl bg-gradient-to-br from-blue-600 to-cyan-600 p-8 text-white overflow-hidden">
        <div className="absolute top-0 right-0 -mt-4 -mr-4 w-32 h-32 bg-white/10 rounded-full blur-2xl"></div>
        <div className="absolute bottom-0 left-0 -mb-4 -ml-4 w-40 h-40 bg-white/10 rounded-full blur-2xl"></div>
        
        <div className="relative z-10">
          <h1 className="text-3xl font-bold mb-2">Välkommen tillbaka! 👋</h1>
          <p className="text-blue-100 mb-6">
            {stats.newThisWeek} nya matchande bidrag denna vecka
          </p>

          <div className="flex flex-wrap gap-4">
            <Button
              size="lg"
              className="bg-white text-blue-600 hover:bg-gray-100"
              onClick={() => window.location.href = '/bidrag'}
            >
              Se nya bidrag
              <ArrowRight className="ml-2 h-5 w-5" />
            </Button>
            <Button
              size="lg"
              variant="outline"
              className="border-white text-white hover:bg-white/10"
              onClick={() => window.location.href = '/applications'}
            >
              Mina ansökningar
            </Button>
          </div>
        </div>
      </div>

      {/* Profile Completion (if not 100%) */}
      {completionProgress < 100 && (
        <Card className="border-amber-200 dark:border-amber-800 bg-amber-50 dark:bg-amber-900/20">
          <CardHeader>
            <CardTitle className="flex items-center gap-2">
              <AlertCircle className="h-5 w-5 text-amber-600" />
              Komplettera din profil
            </CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-gray-600 dark:text-gray-400 mb-4">
              Fyll i mer information för bättre AI-matchning
            </p>
            <Progress value={completionProgress} className="mb-2" />
            <div className="flex justify-between items-center">
              <span className="text-sm font-medium">{completionProgress}% komplett</span>
              <Button size="sm" variant="outline" onClick={() => window.location.href = '/profile'}>
                Komplettera nu
              </Button>
            </div>
          </CardContent>
        </Card>
      )}

      {/* Stats Grid */}
      <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
        <Card>
          <CardHeader className="flex flex-row items-center justify-between pb-2">
            <CardTitle className="text-sm font-medium text-gray-600">
              Totalt bidrag
            </CardTitle>
            <TrendingUp className="h-4 w-4 text-gray-400" />
          </CardHeader>
          <CardContent>
            <div className="text-3xl font-bold">{stats.totalGrants}</div>
            <p className="text-xs text-gray-500 mt-1">
              I vår databas
            </p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader className="flex flex-row items-center justify-between pb-2">
            <CardTitle className="text-sm font-medium text-gray-600">
              Öppna nu
            </CardTitle>
            <CheckCircle className="h-4 w-4 text-green-500" />
          </CardHeader>
          <CardContent>
            <div className="text-3xl font-bold text-green-600">{stats.openGrants}</div>
            <p className="text-xs text-gray-500 mt-1">
              Kan sökas just nu
            </p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader className="flex flex-row items-center justify-between pb-2">
            <CardTitle className="text-sm font-medium text-gray-600">
              Kommande deadlines
            </CardTitle>
            <Clock className="h-4 w-4 text-amber-500" />
          </CardHeader>
          <CardContent>
            <div className="text-3xl font-bold text-amber-600">
              {stats.upcomingDeadlines}
            </div>
            <p className="text-xs text-gray-500 mt-1">
              Inom 7 dagar
            </p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader className="flex flex-row items-center justify-between pb-2">
            <CardTitle className="text-sm font-medium text-gray-600">
              Dina ansökningar
            </CardTitle>
            <Sparkles className="h-4 w-4 text-blue-500" />
          </CardHeader>
          <CardContent>
            <div className="text-3xl font-bold text-blue-600">{stats.applications}</div>
            <p className="text-xs text-gray-500 mt-1">
              Totalt skapade
            </p>
          </CardContent>
        </Card>
      </div>

      {/* Top Matches */}
      <Card>
        <CardHeader>
          <div className="flex items-center justify-between">
            <CardTitle>Dina bästa matchningar</CardTitle>
            <Button variant="ghost" size="sm" onClick={() => window.location.href = '/bidrag'}>
              Se alla
              <ArrowRight className="ml-2 h-4 w-4" />
            </Button>
          </div>
        </CardHeader>
        <CardContent>
          {stats.topMatches.length === 0 ? (
            <div className="text-center py-8 text-gray-500">
              <Sparkles className="h-12 w-12 mx-auto mb-3 text-gray-400" />
              <p>Komplettera din företagsprofil för att se matchningar</p>
              <Button
                variant="outline"
                size="sm"
                className="mt-4"
                onClick={() => window.location.href = '/profile'}
              >
                Gå till profil
              </Button>
            </div>
          ) : (
            <div className="space-y-4">
              {stats.topMatches.slice(0, 5).map((grant: any) => (
                <div
                  key={grant.id}
                  className="flex items-center justify-between p-4 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-blue-500 transition-colors cursor-pointer"
                  onClick={() => window.location.href = `/bidrag/${grant.id}`}
                >
                  <div className="flex-1">
                    <h3 className="font-semibold mb-1">{grant.title}</h3>
                    <div className="flex gap-2 text-sm text-gray-600 dark:text-gray-400">
                      <span>{grant.sourceName}</span>
                      {grant.deadline && (
                        <>
                          <span>•</span>
                          <span>Deadline: {new Date(grant.deadline).toLocaleDateString('sv-SE')}</span>
                        </>
                      )}
                    </div>
                  </div>
                  <div className="text-right ml-4">
                    <div className="text-2xl font-bold text-blue-600">
                      {grant.matchScore}%
                    </div>
                    <div className="text-xs text-gray-500">match</div>
                  </div>
                </div>
              ))}
            </div>
          )}
        </CardContent>
      </Card>

      {/* Quick Actions */}
      <div className="grid gap-6 md:grid-cols-2">
        <Card className="bg-gradient-to-br from-blue-50 to-cyan-50 dark:from-blue-900/20 dark:to-cyan-900/20 border-blue-200 dark:border-blue-800">
          <CardHeader>
            <CardTitle>Sök bidrag</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-gray-600 dark:text-gray-400 mb-4">
              Bläddra bland 379+ aktiva bidrag och hitta de som passar ert företag
            </p>
            <Button className="w-full" onClick={() => window.location.href = '/bidrag'}>
              Utforska bidrag
              <ArrowRight className="ml-2 h-5 w-5" />
            </Button>
          </CardContent>
        </Card>

        <Card className="bg-gradient-to-br from-purple-50 to-pink-50 dark:from-purple-900/20 dark:to-pink-900/20 border-purple-200 dark:border-purple-800">
          <CardHeader>
            <CardTitle>AI-analys</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-gray-600 dark:text-gray-400 mb-4">
              Låt Claude AI analysera vilka bidrag ni har bäst chans att få
            </p>
            <Button
              variant="outline"
              className="w-full"
              onClick={() => window.location.href = '/bidrag?analyze=true'}
            >
              Kör AI-matchning
              <Sparkles className="ml-2 h-5 w-5" />
            </Button>
          </CardContent>
        </Card>
      </div>
    </div>
  );
}
```