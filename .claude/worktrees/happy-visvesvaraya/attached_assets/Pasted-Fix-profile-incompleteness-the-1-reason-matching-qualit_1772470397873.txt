Fix profile incompleteness — the #1 reason matching quality is poor.

Audit shows: 96% of companies missing revenue, 89% missing location, 
93% missing focusAreas. These three fields = 50% of matching score weight.

CHANGE 1 — Onboarding wizard Step 4 (Review step):
After the AI auto-fills company data from the website, explicitly add 
three fields that AI often misses:

A) Revenue range selector (maps to existing revenue field):
   Label: "Ungefärlig årsomsättning"
   Options (select/radio):
   - "Under 1 MSEK"     → value: 500000
   - "1–5 MSEK"        → value: 3000000  
   - "5–20 MSEK"       → value: 12000000
   - "20–100 MSEK"     → value: 60000000
   - "Över 100 MSEK"   → value: 150000000

B) City/region field (maps to existing location field):
   Label: "Var finns företaget?"
   Simple text input with placeholder "t.ex. Stockholm, Göteborg, Malmö"
   Pre-fill from any location the AI extracted, editable

C) Focus areas prompt — if focusAreas is empty after AI extraction:
   Show a tag-input with suggestions based on industry
   Label: "Era fokusområden" 
   Placeholder: "t.ex. AI, hållbarhet, SaaS"

Place these three fields prominently in the Step 4 review card,
BEFORE the existing auto-filled fields (name, description etc).
Mark them with a subtle "Förbättrar din matchning" badge in blue.

CHANGE 2 — Company profile page (/company):
Add a "Matchningsstyrka" card at the TOP of the page (before existing fields).
Show a progress bar and which fields are missing:

  Matchningsstyrka: 45% ████░░░░░░
  
  Lägg till dessa fält för bättre träffar:
  ✗ Omsättning (+20% träffsäkerhet)    [Fyll i]
  ✗ Ort (+15% träffsäkerhet)           [Fyll i]  
  ✗ Fokusområden (+15% träffsäkerhet)  [Fyll i]
  ✓ Bransch                            
  ✓ Antal anställda                    

Calculate the percentage based on which scoring fields are filled:
- industry filled: +30%
- employees filled: +20%  
- revenue filled: +20%
- location filled: +15%
- focusAreas.length > 0: +15%

Clicking [Fyll i] should scroll to that field on the same page.

CHANGE 3 — After saving profile with new fields:
Show a toast: "Profil uppdaterad — dina matchningar har förbättrats"
Then automatically refetch the matches query so updated scores appear.

After implementing, verify:
- Onboarding Step 4 shows the 3 new fields
- Company page shows the matchningsstyrka card
- Saving updates the matching scores
- No TypeScript errors