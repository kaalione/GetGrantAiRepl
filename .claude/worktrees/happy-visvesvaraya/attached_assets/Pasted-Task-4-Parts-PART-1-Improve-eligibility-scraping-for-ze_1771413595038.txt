Task — 4 Parts
PART 1: Improve eligibility scraping for zero-coverage sources
273 grants are missing eligibility data entirely. These scrapers need to be updated to fetch eligibility information from detail pages.
1.1 Länsstyrelserna (98 grants, 0% eligibility)

The scraper fetches title, description, and URL but doesn't visit detail pages
Fix: For each grant, follow the link to the detail page and extract:

Who can apply (target group/eligibility)
Amount/funding level
Geographic scope (which counties)
Application period


Save as JSON in eligibility_criteria:

json{
  "raw_text": "The extracted eligibility text...",
  "target_groups": ["municipalities", "associations"],
  "geography": "Specific county",
  "source_section": "Who can apply"
}

Note: All 21 counties follow similar HTML patterns — one update to the template scraper should cover all

1.2 Jordbruksverket (40 grants, 0% eligibility)

Same problem — detail pages not visited
Fix: Follow each grant's URL to the detail page. Jordbruksverket's support pages typically have sections like "Vem kan söka?" (Who can apply?), "Vilka krav finns?" (What requirements exist?), "Hur mycket kan du få?" (How much can you get?)
Extract these sections and save as eligibility_criteria JSON

1.3 Verksamt.se (30 grants, 0% eligibility)

Verksamt aggregates from other agencies — detail pages often have "Vem riktar sig stödet till?" (Who is the support aimed at?)
Fix: Scrape detail pages for each grant. Verksamt.se is JS-rendered (Playwright required)
Extract eligibility sections

1.4 Boverket (11 grants, 0% eligibility)

Fix: Visit detail pages, extract "Vem kan söka?" sections

1.5 Partial coverage sources — improve
These have eligibility for SOME grants but not all:
SourceHasMissingActionEnergimyndigheten24/39 (62%)15Re-run scraper on grants without eligibilityKK-stiftelsen6/13 (46%)7Scrape detail pages for remainingVGR3/9 (33%)6Scrape detail pagesRegion Skåne8/27 (30%)19Scrape detail pagesKulturrådet3/12 (25%)9Scrape detail pagesNaturvårdsverket1/6 (17%)5Scrape detail pagesTillväxtverket2/13 (15%)11Scrape detail pagesEufonder.se1/20 (5%)19Scrape detail pages
Goal: Go from 83% to 95%+ eligibility coverage

PART 2: AI Eligibility Extraction — Standardize ALL data
The problem: Even the 1,346 grants with eligibility data have completely different formats:

Almi: Nice key-value pairs in Swedish
Vinnova: Just metadata flags ("public": true, "diarienummer": "...") — not real eligibility criteria
EU F&T: English descriptions with varying structure
Interreg: Mixed formats

Solution: Run the AI extraction pipeline on ALL grants and save standardized output.
2.1 Schema for structured eligibility
There's already an eligibility_criteria JSONB column. Do NOT run AI extraction and overwrite existing data. Instead:
Option A (recommended): Add a new column structured_eligibility (JSONB) containing the standardized AI-extracted result. Keep eligibility_criteria as raw/original data.
sqlALTER TABLE grants ADD COLUMN IF NOT EXISTS structured_eligibility JSONB;
ALTER TABLE grants ADD COLUMN IF NOT EXISTS eligibility_extracted_at TIMESTAMP;
Option B (if you don't want to change schema): Save AI output in eligibility_criteria but with a clear wrapper format:
json{
  "raw": "Original text...",
  "structured": { /* AI-extracted */ },
  "extracted_at": "2026-02-18T..."
}
2.2 Standardized output format (target schema)
AI extraction should produce this JSON format for EVERY grant:
json{
  "company_types": ["aktiebolag", "enskild_firma"],
  "company_sizes": {
    "min_employees": null,
    "max_employees": 250,
    "size_category": ["micro", "small", "medium"],
    "description": "SME companies"
  },
  "geography": {
    "regions": ["hela_sverige"],
    "eu_countries": false,
    "description": ""
  },
  "sectors": ["cleantech", "energy"],
  "collaboration_required": {
    "required": false,
    "partner_types": [],
    "description": ""
  },
  "funding_details": {
    "min_amount": null,
    "max_amount": 5000000,
    "co_financing_required": true,
    "co_financing_percentage": 50,
    "description": "Max 5 MSEK, 50% co-financing required"
  },
  "other_requirements": ["Must have Swedish F-tax certificate"],
  "who_cannot_apply": ["Individuals", "Municipalities"],
  "confidence_score": 0.85,
  "source_text_used": "Most relevant sentence..."
}
2.3 Processing order
Prioritize extraction in this order:

Open grants with raw eligibility text (most valuable data to structure) — ~1,100
Open grants without eligibility (AI extracts from description instead) — ~190
Closed grants with eligibility (lower priority, run if time permits)

2.4 Implementation

Verify that eligibilityExtractor.ts exists and works
Update it to save to the structured_eligibility column (or wrapper format)
Test with 5 grants from different sources:

1 EU F&T grant (English text)
1 Vinnova grant (metadata format — AI needs to extract from description instead)
1 Almi grant (already good data — verify AI doesn't make it worse)
1 Länsstyrelse grant (no eligibility — extract from description)
1 Interreg grant (mixed format)


Manually review output quality
Then run in batches:

typescriptawait processAllGrants({
  batchSize: 3,
  delayBetweenBatchesMs: 15000,
  onlyOpen: true,         // Prioritize open grants
  forceReprocess: false,  // Skip already processed
  maxGrants: 50           // Per cron run
});

Integrate into cron: run daily after scraping (scraping 08:00, extraction 09:00)
Create admin endpoints:

POST /api/admin/extract-eligibility — manual run
POST /api/admin/extract-eligibility/:grantId — single grant
GET /api/admin/eligibility-status — statistics



2.5 Special handling per source
Vinnova grants have eligibility_criteria that's just metadata ("public": true). This is NOT real eligibility criteria. For these:

Ignore existing eligibility_criteria
Extract from the description field instead
Flag: "extraction_source": "description" in output

EU F&T grants have English text. The AI prompt must handle BOTH Swedish and English input.
Cost estimate:

~1,300 grants to process
~$0.005-0.01 per grant (Claude Sonnet)
Total: ~$6.50-13 one-time cost
Daily cost for new grants: ~$0.05-0.10/day


PART 3: Default filters — Show only relevant grants
3.1 Status filter (open/upcoming as default)
Problem: Vinnova has 361 grants but only 50 are open. Closed grants confuse users.
Fix — Backend:

Grant listing endpoints: default status filter = ['open', 'upcoming']
Accept query param ?status=all to show everything
Sort order: open first, then upcoming, then closed. Within each group: deadline ASC

sqlWHERE status IN ('open', 'upcoming')  -- default
ORDER BY 
  CASE WHEN status = 'open' THEN 0 
       WHEN status = 'upcoming' THEN 1 END,
  deadline ASC NULLS LAST;
Fix — Frontend:

StatusFilter component with pills/chips: Open ✓ | Upcoming ✓ | Closed ○
Default: Open + Upcoming checked
Closed grants: render with opacity-60, gray "Closed" badge, sorted to bottom
Show count per status: "Open (1,289)" etc.

3.2 Profile matching filter
Requires: PART 2 (structured eligibility) to be completed first.
Fix — Backend:

If user has company_profile AND grants have structured_eligibility:

Calculate match_score based on: company_type, size_category, geography, sectors
Scoring: +25 points per matching dimension (max 100)
Grants without structured_eligibility: give neutral score 50 (don't exclude them)


Endpoint: GET /api/grants?matchProfile=true
Return match_score in response

Fix — Frontend:

Toggle above grant list: "Show matching" (default if profile exists) / "Show all"
If no profile: show banner "Create a company profile to see matching grants →"
Matching view: sort by match_score DESC, show score badge per grant
"Show all" view: sort by deadline ASC (current behavior)


PART 4: Minor fixes
4.1 Internetstiftelsen scraper
Registered but shows 0 grants in the count. Debug:

Run scraper manually and log output
Check URL: https://internetstiftelsen.se/sok-stod/
Likely problem: selectors don't match or scraper returns empty
Should yield 3-8 grants

4.2 Boverket status mapping
11 grants but only 1 open. Check:

Are the other 10 actually closed? Visit Boverket's site manually
Likely problem: status mapping defaults to "closed" if nothing explicit is found
Fix: Update status logic. If deadline is missing or in the future → "open"

4.3 Stiftelsemedel.se external link
Add a section on the grants page (NOT as a scraper):

Card: "🔍 Find more foundations"
Text: "Search among 17,000+ foundations"
Two links:

https://stiftelsemedel.se/
https://stiftelser.lansstyrelsen.se/


Placement: below the grant list or in sidebar


Implementation Order
StepPartDescriptionEstimate14.1-4.2Fix Internetstiftelsen + Boverket status1-2h21Improve eligibility scraping (zero + partial coverage)6-10h32.1-2.3Schema + AI extraction setup + test with 5 grants2-3h42.4-2.5Run AI extraction on all open grants3-4h (+ API time)53.1Status filter (open/upcoming default)2-3h63.2Profile matching filter4-6h74.3Stiftelsemedel external link30min
Total: ~20-30 hours
Verification
Run these queries after implementation:
sql-- 1. Eligibility coverage
SELECT 
  COUNT(*) as total,
  COUNT(*) FILTER (WHERE eligibility_criteria IS NOT NULL 
    AND eligibility_criteria::text NOT IN ('null', '{}', '""', '[]')) as has_raw_eligibility,
  COUNT(*) FILTER (WHERE structured_eligibility IS NOT NULL) as has_structured,
  ROUND(100.0 * COUNT(*) FILTER (WHERE structured_eligibility IS NOT NULL) / COUNT(*), 1) as structured_pct
FROM grants WHERE status = 'open';

-- 2. Structured eligibility per source
SELECT 
  source_name,
  COUNT(*) as total,
  COUNT(*) FILTER (WHERE structured_eligibility IS NOT NULL) as structured,
  ROUND(100.0 * COUNT(*) FILTER (WHERE structured_eligibility IS NOT NULL) / COUNT(*), 1) as pct
FROM grants 
WHERE status = 'open'
GROUP BY source_name 
ORDER BY pct ASC;

-- 3. Status distribution
SELECT status, COUNT(*) FROM grants GROUP BY status ORDER BY COUNT(*) DESC;

-- 4. Verify Boverket fix
SELECT title, status, deadline FROM grants WHERE source_name ILIKE '%boverket%';

-- 5. Verify Internetstiftelsen
SELECT COUNT(*) FROM grants WHERE source_name ILIKE '%internetstiftelsen%';

-- 6. Sample structured eligibility quality
SELECT title, source_name, structured_eligibility 
FROM grants 
WHERE structured_eligibility IS NOT NULL 
ORDER BY RANDOM() LIMIT 5;
Expected End Result
MetricBeforeAfterEligibility coverage (raw)83% (1,346/1,619)95%+ (~1,540/1,619)Structured eligibility0%80%+ of open grantsDefault view showsAll 1,619 (confusing)Only open matching grantsInternetstiftelsen grants03-8Boverket open18-10