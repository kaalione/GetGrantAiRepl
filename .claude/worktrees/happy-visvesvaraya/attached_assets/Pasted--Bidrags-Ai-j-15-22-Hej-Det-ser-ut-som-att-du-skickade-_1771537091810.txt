
Bidrags Ai
/
j

15:22
Hej! Det ser ut som att du skickade bara ett "j" — menade du

Claude's response was interrupted
jag har precis noterat att det finns en annan tjänst som heter getgrant.eu som verkar ha liknande plattform som jag. så jag undar dels om jag borde byta namn från getgrant.ai? Sedan skulle jag vilja att du går igenom deras hemsida i detalj för att se om de väcker kompletterande idéer samt om du kan se alla deras källor så jag kan addera flera.

15:25
Intressant fynd! Låt mig göra en djupdykning på getgrant.eu för att kartlägga deras plattform, källor och funktioner ordentligt. Jag startar forskningen nu!


GetGrant.eu competitive analysis
favicon
favicon
favicon
Research complete
•
201 sources
•
5m 55s
Your competitive analysis report on GetGrant.eu is ready. It covers the platform's Ukrainian origins, AI-powered grant matching features, comprehensive inventory of 90+ funding sources, pricing structure ($12/month), and consulting services. The report includes a detailed assessment of the naming conflict with GetGrant.ai, trademark considerations, Swedish market coverage gaps (virtually zero domestic Swedish grants), and actionable takeaways for building a Swedish-focused grant discovery platform.

GetGrant.eu: Ukrainian AI Grant Platform Competitive Analysis and Naming Overlap Risk Assessment
Document 
based on these insights, any recoomendations to the getgrant.ai platform in terms of features or grant sources

16:17
Baserat på analysen av GetGrant.eu och vad du redan byggt, här är mina konkreta rekommendationer:

Funktioner att inspireras av (och förbättra)
Konsortiumpartner-matchning är deras starkaste differentiator. De har 80 000+ partners i sin databas för EU-projekt. För GetGrant.ai skulle en nordisk variant — Svenska, norska och danska företag sökbara per bransch, TRL-nivå och projektroll — vara ett kraftfullt tillägg, särskilt för Horizon Europe och Interreg-ansökningar som kräver gränsöverskridande konsortier.

Relevansscore i procent är intuitivt och bygger förtroende. Du har redan AI-matchning, men att exponera en explicit score (t.ex. "87% match") ger användaren en omedelbar signal och gör att din matchningsalgoritm känns konkret snarare än en svart låda.

Tjänsteskikt ovanpå plattformen — GetGrant.eu erbjuder konsulttjänster (ansökningsskrivning, budgetrådgivning, revision) som ett komplement till SaaS. Många svenska SMF:er saknar intern kapacitet att faktiskt skriva en vinnande Vinnova-ansökan. En marknadsplats där certifierade konsulter kan kopplas till användare via GetGrant.ai skulle skapa en ny intäktsström och öka plattformens värde avsevärt.

Vad de saknar som du bör bygga:

AI-assisterad ansökningsskrivning (Cogrant.eu och Granter.ai börjar erbjuda detta — det är dit marknaden rör sig)
Deadline-kalender med synk till Google/Outlook
Kollaborativt arbetsläge där ett team kan jobba på samma ansökan
Mobilvänlig upplevelse
Integration med officiella inlämningsportaler (Vinnova, Tillväxtverket m.fl.)
Bidragskällor att lägga till
GetGrant.eu täcker inga svenska nationella källor alls — det är din unika position. Men de täcker ett antal EU-program och internationella organisationer du ännu inte har. Prioriterat:

Högt värde, sannolikt saknas i din databas idag:

Alla nio EIT Knowledge & Innovation Communities är öppna för svenska företag och har egna utlysningar: EIT Urban Mobility, EIT Manufacturing, EIT Food, EIT Health, EIT Digital, EIT RawMaterials, EIT Climate-KIC, EIT Culture & Creativity, och EIT InnoEnergy. Dessa är ofta förbisedda av svenska bolag men relevanta för deep tech och hållbarhet.

CASSINI Business Accelerator — EU:s rymd-startup-program via ESA/EUSPA. Mycket relevant för tech-bolag och relativt okänt i Sverige.

Innovativa Health Initiative (IHI) — EU:s public-private partnership för hälsodata och life science. Relevant för HealthTech-segmentet.

Nordiska rådet/Nordic Council of Ministers — "Prosperous Future"-programmet och Nordic Innovation-utlysningar. Tydlig geografisk relevans men ofta saknad i svenska aggregatorer.

COST Actions — nätverksbidrag för forskningssamarbete. Öppet för svenska SMF:er med FoU-fokus.

NLnet Foundation (NGI Zero) — privat stiftelse som finansierar open source och privacy-tech. Relevant för tech-startups.

NATO Science for Peace and Security — relevant för dual-use och försvarstech-bolag, ett växande segment i Sverige post-2022.

Brave1 / försvarstech-ekvivalenter — om du vill ha ett försvarstech-spår, finns EU-programmet EDIDP/EDF (European Defence Fund) som är öppet för svenska försvarsbolag.

Den ena rekommendationen som ger störst effekt
Om jag ska peka ut en sak: bygg EIT-integrationen. Nio separata program, alla med liknande ansökningslogik, alla öppna för svenska bolag, alla underrepresenterade i befintliga svenska aggregatorer. En enda scraper-mall täcker nio källor och ger dig en unik täckning som ingen svensk konkurrent har idag.

super tack, kan du skriva ihop ett replit prompt som integrerar alla följande källor Alla nio EIT Knowledge & Innovation Communities är öppna för svenska företag och har egna utlysningar: EIT Urban Mobility, EIT Manufacturing, EIT Food, EIT Health, EIT Digital, EIT RawMaterials, EIT Climate-KIC, EIT Culture & Creativity, och EIT InnoEnergy. Dessa är ofta förbisedda av svenska bolag men relevanta för deep tech och hållbarhet.
CASSINI Business Accelerator — EU:s rymd-startup-program via ESA/EUSPA. Mycket relevant för tech-bolag och relativt okänt i Sverige.
Innovativa Health Initiative (IHI) — EU:s public-private partnership för hälsodata och life science. Relevant för HealthTech-segmentet.
Nordiska rådet/Nordic Council of Ministers — "Prosperous Future"-programmet och Nordic Innovation-utlysningar. Tydlig geografisk relevans men ofta saknad i svenska aggregatorer.
COST Actions — nätverksbidrag för forskningssamarbete. Öppet för svenska SMF:er med FoU-fokus.
NLnet Foundation (NGI Zero) — privat stiftelse som finansierar open source och privacy-tech. Relevant för tech-startups.
NATO Science for Peace and Security — relevant för dual-use och försvarstech-bolag, ett växande segment i Sverige post-2022.
Brave1 / försvarstech-ekvivalenter — om du vill ha ett försvarstech-spår, finns EU-programmet EDIDP/EDF (European Defence Fund) som är öppet för svenska försvarsbolag.

Show more
16:52
Låt mig först undersöka alla dessa källors webbplatser för att hitta rätt URL:er och scraping-strategi innan jag skriver prompten.


Searched the web
Searched the web







Perfekt, nu har jag all nödvändig information. Låt mig skriva prompten.


Read project files to understand existing scraper architecture
Read project files to understand existing scraper architecture
Nu har jag allt jag behöver. Här är en komplett, copy-paste-klar Replit-prompt:

Replit-prompt: Nya bidragskällor — EIT, CASSINI, IHI, Nordic, COST, NLnet, NATO & EDF
Jag behöver att du lägger till 8 nya scraper-källor till GetGrant.ai-plattformen. 
Nedanför hittar du exakt URL, teknik och detaljer för varje källa. Följ den 
befintliga scraper-arkitekturen (base_scraper.py / api_client.py) och säkerställ 
att varje scraper upsert:ar bidrag via den befintliga upsert_grant-funktionen med 
korrekt deduplicering.

=================================================================
KÄLLA 1 — EIT Knowledge & Innovation Communities (9 KICs i en scraper)
=================================================================
Alla 9 EIT KICs har utlysningssidor med liknande HTML-struktur. Bygg EN generisk 
EitKicScraper-klass som tar en config-lista och loopar över alla nio:

EIT_KICS = [
  { id: "eit-urban-mobility",    url: "https://www.eiturbanmobility.eu/call-for-proposals/",        label: "EIT Urban Mobility",     sector: "Transport/Mobilitet" },
  { id: "eit-manufacturing",     url: "https://eitmanufacturing.eu/what-we-offer/open-calls/",       label: "EIT Manufacturing",      sector: "Tillverkning/Industri" },
  { id: "eit-food",              url: "https://www.eitfood.eu/open-calls",                           label: "EIT Food",               sector: "Livsmedel/Agritech" },
  { id: "eit-health",            url: "https://eithealth.eu/programmes-projects/",                   label: "EIT Health",             sector: "Hälsa/Life Science" },
  { id: "eit-digital",           url: "https://www.eitdigital.eu/our-work/calls/",                   label: "EIT Digital",            sector: "Digitalt/AI" },
  { id: "eit-rawmaterials",      url: "https://eitrawmaterials.eu/funding-opportunities/",           label: "EIT RawMaterials",       sector: "Råmaterial/Gruvteknik" },
  { id: "eit-climate-kic",       url: "https://www.climate-kic.org/open-calls/",                     label: "EIT Climate-KIC",        sector: "Klimat/Hållbarhet" },
  { id: "eit-culture-creativity",url: "https://eitculture.eu/open-calls/",                           label: "EIT Culture & Creativity", sector: "Kultur/Kreativa industrier" },
  { id: "eit-innoenergy",        url: "https://www.eitinnoenergy.com/for-innovators/calls/",         label: "EIT InnoEnergy",         sector: "Energi/CleanTech" }
]

Teknik: BeautifulSoup (BS4). För varje KIC: scrapa titlar, beskrivningar, 
deadlines och belopp från utlysningssidan. Tagga source_id som t.ex. 
"eit-urban-mobility", sätt funding_type = "EU/EIT", geography = "EU/Sverige".

Notera: Sidornas HTML-struktur skiljer sig något åt per KIC. Bygg robusta 
fallback-selectors. Om en sida kräver JS-rendering, byt till Playwright för 
just den KIC:en. Logga vilken teknik som används per källa.

=================================================================
KÄLLA 2 — CASSINI Business Accelerator (ESA/EUSPA, rymd-startup)
=================================================================
Primär URL: https://www.cassini.eu/accelerator/
Kompletterande: https://www.euspa.europa.eu/opportunities/for-startups

Teknik: BS4 (statisk sida)
Scrapa: Öppna ansökningsomgångar för CASSINI Business Accelerator + 
CASSINI Challenges + CASSINI Hackathons.
source_id: "cassini-euspa"
funding_type: "EU/Rymd"
sector: "Space Tech/Deep Tech"
geography: "EU/Sverige"
Notering: Programmet är återkommande med ca 2 omgångar per år. Hämta 
deadline, belopp (acceleratorn erbjuder €75,000 voucher + investeringsstöd), 
och länk till ansökan.

=================================================================
KÄLLA 3 — IHI (Innovative Health Initiative)
=================================================================
Primär URL: https://www.ihi.europa.eu/apply-funding/open-calls
Framtida calls: https://www.ihi.europa.eu/apply-funding/future-opportunities

Teknik: BS4
source_id: "ihi-eu"
funding_type: "EU/IHI"
sector: "Hälsa/Life Science/HealthTech"
geography: "EU/Sverige"
Scrapa: Alla öppna calls (t.ex. IHI Call 12 som är öppen nu med deadline 
ca April 2026). Hämta call-nummer, ämnesområden (topics), deadline, budget 
per projekt, och länk. IHI calls är stora (>€100M totalt), men individuella 
projekt är typiskt €5-30M — ange detta tydligt i beskrivningen.
Notering: IHI är ett public-private partnership under Horizon Europe. 
Svenska företag och forskningsinstitut är fullt berättigade.

=================================================================
KÄLLA 4 — Nordic Innovation (Nordiska rådet)
=================================================================
Primär URL: https://www.nordicinnovation.org/programs
Kompletterande (NordForsk): https://www.nordforsk.org/funding-opportunities

Teknik: BS4
source_id: "nordic-innovation"
funding_type: "Nordiskt/Mellanstatligt"
sector: "Hållbarhet/Innovation/Deep Tech"
geography: "Norden/Sverige"
Scrapa: Alla öppna calls under nordicinnovation.org/programs. Typiska 
program: "Innovative Solutions for 2030", "Nordic Forward: Resilience 
and Competitiveness for 2050". Funding i NOK (konvertera eller visa i NOK 
med tydlig label). Lägg även till NordForsk om öppna calls finns.
Notering: Kräver minst 3 nordiska länder i konsortiet. Tagga detta som 
eligibility-krav.

=================================================================
KÄLLA 5 — COST Actions (European Cooperation in Science and Technology)
=================================================================
Primär URL: https://www.cost.eu/funding/open-call-a-simple-one-step-application-process/
Öppna actions-databas: https://www.cost.eu/cost-actions/

Teknik: BS4 + eventuellt e-COST API om tillgängligt
source_id: "cost-actions"
funding_type: "EU/COST"
sector: "FoU/Nätverk/Tvärvetenskapligt"
geography: "EU/Sverige"

Scrapa TWÅ saker:
1. Öppna call för NYA actions (Open Call 2026 — ansökan öppnar sommar 2026, 
   deadline höst 2026). Budget: €125,000/år i 4 år per action.
2. Befintliga LÖPANDE COST Actions som fortfarande rekryterar nya 
   medlemmar — sök på https://e-cost.cost.eu/costActionSearch med status 
   "OPEN" eller via https://www.cost.eu/cost-actions/ filtrera på aktiva.
   
Notering: COST Actions är nätverksbidrag, inte projektbidrag. Tydliggör 
detta i funding_type och beskrivning. Inga direkta EU-pengar till organisationen, 
men möjlighet att delta i pan-europeiska FoU-nätverk med reseersättning etc.

=================================================================
KÄLLA 6 — NLnet Foundation / NGI Zero
=================================================================
Primär URL: https://nlnet.nl/commonsfund/
Aktiva program: https://nlnet.nl/NGI0/

Teknik: BS4
source_id: "nlnet-foundation"
funding_type: "Stiftelse/NGI"
sector: "Open Source/Privacy Tech/Internet-infrastruktur"
geography: "Europa/Sverige (Horizon Europe-länder prioriteras)"

Scrapa: Alla öppna calls under nlnet.nl. Idag aktivt:
- NGI Zero Commons Fund (öppen call var 2:a månad, €5,000–€50,000 per projekt)
- NGI TALER (digitala betalningssystem)
- NGI Fediversity (hosting/cloud)

Hämta: deadline per call-omgång, max belopp, krav (FOSS-licens obligatorisk), 
länk till ansökan. Notera att calls öppnar var annan månad — scrapa detta schema.
Viktig eligibility-flagg: projekt MÅSTE levereras som fri/öppen källkod.

=================================================================
KÄLLA 7 — NATO Science for Peace and Security (SPS)
=================================================================
Primär URL: https://www.nato.int/en/about-us/organization/nato-structure/science-for-peace-and-security-hub/sps-call-for-proposals-2026-1
SPS Hub: https://www.nato.int/cps/en/natohq/78209.htm

Teknik: BS4
source_id: "nato-sps"
funding_type: "NATO/Försvarsrelaterat"
sector: "Cybersäkerhet/Dual-use/Försvarstech/CBRNE"
geography: "NATO-länder/Sverige"

Scrapa: Öppna calls för proposals (vanligtvis 2 per år: "2026-1", "2026-2").
Grant-typer:
- Multi-Year Projects (MYP): €200,000–€2M, 2-3 år
- Advanced Research Workshops (ARW): ca €60,000
- Advanced Training Courses (ATC): ca €60,000

Hämta: Aktiva calls med deadline, budget, SPS Key Priorities (t.ex. 
Cyber Defence, Counter-Terrorism, Advanced Technologies, Energy Security).
Viktig flagg: Kräver minst EN NATO-nation + EN partner-nation i projektet. 
Sverige är NATO-land sedan 2024 — tydliggör detta i eligibility.

=================================================================
KÄLLA 8 — EDF (European Defence Fund)
=================================================================
Primär URL: https://edf.euspa.europa.eu/ 
      ELLER https://defence-industry-space.ec.europa.eu/eu-defence-industry/european-defence-fund_en
Calls via EU F&T Portal: Filtrera på programme = "EDF" på 
https://ec.europa.eu/info/funding-tenders/opportunities/portal/screen/opportunities/topic-search

Teknik: Lägg till EDF som ett filter i den befintliga EU Funding & Tenders 
scraper/API-klienten. Om EU F&T-scraper redan finns: lägg till EDF i 
programme-filtret. Om inte: bygg ny eu_funding_tenders.py med EDF-filter.
source_id: "edf-eu"
funding_type: "EU/Försvars-FoU"
sector: "Försvarstech/Dual-use/Säkerhetsteknik"
geography: "EU/Sverige"

Scrapa: Öppna EDF-calls (Work Programme 2026 är nu publicerat). Budget 
per call varierar kraftigt (€5M–€200M). Hämta call-titel, deadline, 
budget, kort beskrivning, och direktlänk till EU F&T Portal för ansökan.
Viktig eligibility-flagg: Kräver minst 3 legala enheter från 3 olika 
EU-länder. Inga tredjelandsaktörer. Sverige fullt berättigat.

=================================================================
GEMENSAMMA TEKNISKA KRAV FÖR ALLA 8 KÄLLOR
=================================================================

1. DATASTRUKTUR: Varje bidrag ska mappas till befintligt grant-schema:
   - title (str)
   - description (str, max 2000 tecken, svensk eller engelsk)
   - source_id (str, enligt ovan)
   - source_url (str, direktlänk till utlysningen)
   - funding_amount_min / funding_amount_max (int, EUR)
   - deadline (date eller null)
   - status: "open" | "upcoming" | "closed"
   - funding_type (str)
   - sector_tags (array of strings)
   - geography (str)
   - eligibility_notes (str, kortfattat om krav)
   - language: "en" (alla dessa källor är på engelska)

2. DEDUPLICERING: Använd befintlig 3-nivå dedup (URL-match → titel-similarity 
   → content hash). Dessa är EU-källor utan överlapp med svenska myndigheter, 
   men EDF kan överlappa med EU F&T Portal.

3. SCHEMALÄGGNING: Lägg till alla 8 scrapers i den befintliga 
   scheduler/cron-konfigurationen. Rekommenderad frekvens:
   - EIT KICs: varannan vecka (calls öppnar sällan)
   - CASSINI: månadsvis
   - IHI: varannan vecka (calls pågår månader)
   - Nordic Innovation: varannan vecka
   - COST Actions: månadsvis
   - NLnet/NGI Zero: varannan månad (matchar deras call-rytm)
   - NATO SPS: månadsvis
   - EDF: varannan vecka

4. FELHANTERING: Om en sida inte svarar eller HTML-strukturen förändrats, 
   logga felmeddelandet med source_id + timestamp + HTTP-kod och skicka 
   alert via befintligt monitoring-system. Markera INTE befintliga bidrag 
   som "closed" förrän 3 misslyckade scraping-försök i rad.

5. TAGGNING FÖR AI-MATCHNING: Lägg till följande sector_tags per källa 
   för att förbättra AI-matchningens precision:
   - EIT Urban Mobility: ["transport", "mobilitet", "stadsplanering", "elfordon"]
   - EIT Manufacturing: ["tillverkning", "industri", "automation", "AI"]
   - EIT Food: ["livsmedel", "agritech", "hållbart jordbruk", "matproduktion"]
   - EIT Health: ["hälsa", "life science", "medtech", "sjukvård"]
   - EIT Digital: ["digitalt", "AI", "cybersäkerhet", "deeptech"]
   - EIT RawMaterials: ["råmaterial", "gruvindustri", "cirkular ekonomi"]
   - EIT Climate-KIC: ["klimat", "hållbarhet", "cleantech", "CO2"]
   - EIT Culture & Creativity: ["kultur", "kreativa industrier", "media"]
   - EIT InnoEnergy: ["energi", "cleantech", "batterier", "vätgas"]
   - CASSINI: ["space tech", "satellit", "rymdfart", "geospatialt"]
   - IHI: ["hälsa", "life science", "pharma", "healthtech", "klinisk forskning"]
   - Nordic Innovation: ["nordisk", "hållbarhet", "innovation", "SMF"]
   - COST Actions: ["forskning", "nätverk", "akademi", "tvärvetenskapligt"]
   - NLnet/NGI Zero: ["open source", "privacy", "internet-infrastruktur", "FOSS"]
   - NATO SPS: ["cybersäkerhet", "försvar", "dual-use", "säkerhet"]
   - EDF: ["försvar", "dual-use", "säkerhetsteknik", "försvarstech"]

6. TESTNING: Efter implementation, kör varje scraper manuellt och 
   verifiera att minst 1 bidrag hämtas per källa. Logga antal hämtade 
   bidrag per körning. Om 0 bidrag returneras — flagga som ERROR, 
   inte silent success.

Rapportera tillbaka med: 
- Antal bidrag hämtade per källa vid första körning
- Eventuella JS-renderingsproblem (Playwright behövs)  
- HTML-strukturer som avviker från förväntad (fallback-selectors aktiverade)
- Totalt antal nya bidrag adderade till databasen
Några saker att notera kring prompten:

EIT-scraper-strukturen är medvetet generisk — det sparar dig mycket tid. En loop över 9 KICs med config-objekt är mer underhållsvänligt än 9 separata filer, även om HTML-strukturen skiljer sig lite åt.

NLnet är den source som är mest nischad men har en unik egenskap — calls öppnar var annan månad och stänger inom 2 månader. Det är värt att exponera detta schema tydligt i UI:t så att användare förstår tidskritikaliteten.

NATO SPS är politiskt känsligare att ha med i en civil plattform, men sedan Sverige gick med i NATO 2024 är det en legitim och växande funding-källa för svenska tech-bolag, särskilt inom cyber och dual-use. Prompten flaggar detta tydligt i eligibility-taggen.

EDF är sannolikt redan delvis täckt av din befintliga EU F&T Portal-scraper — det är därför prompten ber Replit att kontrollera om det räcker med att lägga till ett filter istället för att bygga en ny scraper från grunden.

gå igenom följande rapporter/blogg poster och gör en gedigen konkurrens analys för getgrant.ai och jämför erbjudande, affärsmodell, sources m.m och se hur getgrant förhåller sig till befintliga konkurrenter samt om det finns inspiration att implementera in i getgrant.ai https://www.cogrant.eu/blog/articles/we-tested-21-ai-tools-for-grant-writing-here-s-which-tool-is-the-best-in-the-world https://atomgrants.com/blog/best-grant-databases-2026

17:08
Det här är en tydlig uppgift — jag sätter igång med en djupgående analys direkt. Jag ska gå igenom båda länkarna noggrant och bygga en gedigen konkurrensanalys för GetGrant.ai.


GetGrant.ai competitive analysis
favicon
favicon
favicon
Research complete
•
523 sources
•
12m 57s
Your competitive analysis of GetGrant.ai in the global grant-tech landscape is ready. It covers market segmentation across geography, target audience, and core functionality, with a detailed comparison of 17+ platforms including pricing, AI capabilities, and Swedish/EU source coverage. The report features deep dives into the five most relevant competitors (GrantPilot.se, Cogrant.eu, Granter.ai, Instrumentl, and Bidragsportalen.se), a feature prioritization roadmap inspired by competitor capabilities, pricing recommendations for the Swedish SME market, a source-coverage gap analysis, and strategic recommendations for defending and expanding GetGrant.ai's position as the only AI-driven platform with deep Swedish grant coverage.

Konkurrensanalys: GetGrant.ai i det globala bidragstech-landskapet
Document 

Let me start building all 8 scrapers. I'll work on multiple in parallel using subagents for efficiency. Let me create the first 4 scrapers in parallel: Good, first 4 scrapers created. Now let me create the remaining 4 scrapers: Good, all 7 scraper files are created. The LSP warnings are expected

pasted

jag körde prompten för replit o nya källor och fick följande response - något som borde förbättras?

17:22
Bra resultat generellt – scraper-arkitekturen ser solid ut. Men det finns flera konkreta saker att förbättra. Låt mig gå igenom dem:

🔴 Kritiska problem
1. EIT KICs levererar bara 12 bidrag för 9 källor

Det är nästan ingenting. Problemet är JS-rendering – de flesta EIT-sidor kräver Playwright men Replit använde BeautifulSoup. Prompten specificerade faktiskt detta ("Om en sida kräver JS-rendering, byt till Playwright") men det verkar inte ha implementerats för mer än EIT Urban Mobility. 12 bidrag från 9 KICs är i princip failure state.

2. IHI fick bara 2 bidrag via "known programs fallback"

"SSL issues with live site, known programs loaded" är ett rött flagg. Det betyder att scraper:n inte faktiskt hämtar data från ihi.europa.eu utan faller tillbaka på hårdkodade värden. IHI Call 12 är öppen just nu med deadline ~april 2026 och inga av de aktiva callsen verkar ha blivit korrekt scrapade.

3. NLnet scraper "scraping too many navigation links"

21 "bidrag" men med kvalitetsproblem – navigeringslänkar räknas som bidrag. Det behöver rensas med bättre selectors.

4. Totala antalet sjönk jämfört med förväntat

Gick från ~1 350 → ~1 460 bidrag. Prompten estimerade ~400 nya bidrag (EDF ~27 + NATO ~24 + Nordic ~10 + COST ~10 + NLnet ~21 + IHI ~2 + CASSINI ~5 + EIT ~12 = ~111). Det är dramatiskt under de ~300–400 som var förväntat, framför allt pga EIT-problemet.

🟠 Medelallvarliga problem
5. Antal sources uppdaterades inkonsekvent

Landing page säger "35+ sources" men rapporten säger 37 och scraper_sources har 37 registrerade. Välj ett nummer och håll det konsistent överallt (landing page, replit.md, translations, statsbar).

6. Scrapers med 0 bidrag (EIT Health, EIT Digital – 404) är registrerade i databasen

Att ha aktiva scrapers med 404-fel utan åtgärdsplan är tech debt. Antingen bör dessa pausas tills URL:erna är korrekta, eller så bör monitoring-systemet flagga dem med en tydlig "URL_CHANGED" status.

7. EDF är en tunn wrapper runt befintlig EU F&T-scraper

Det är smart arkitektur, men det betyder att EDF-bidragen sannolikt duppliceras om EU F&T-scraper:n körs igen utan korrekt deduplicering. Verifiera att EDF-wrappern inte skapar dubbletter.

🟡 Förbättringsförslag
8. Kör en korrigerings-prompt direkt

Här är vad du bör skicka till Replit nu:

Följande problem behöver åtgärdas i de nya scrapers:

1. EIT KICs (kritiskt): 7 av 9 KICs returnerar 0 bidrag pga JS-rendering.
   Byt till Playwright för följande KICs:
   - EIT Health (eithealth.eu)
   - EIT Digital (eitdigital.eu)
   - EIT Manufacturing (eitmanufacturing.eu)
   - EIT RawMaterials (eitrawmaterials.eu)
   - EIT InnoEnergy (eitinnoenergy.com)
   - EIT Culture & Creativity (eitculture.eu)
   - EIT Climate-KIC (climate-kic.org)
   
   Hämta och verifiera korrekt URL för calls/open-calls-sidan på varje KIC.
   Målet är minst 3-5 bidrag per KIC, totalt 30+ för alla 9.

2. IHI (kritiskt): Scraper:n faller tillbaka på hårdkodade "known programs" 
   istället för att hämta live data. Fixa SSL-problemet (acceptera certifikat 
   eller använd requests med verify=False + varning i log) och hämta aktiva 
   calls från https://www.ihi.europa.eu/apply-funding/open-calls
   IHI Call 12 är aktiv nu med deadline i april 2026.

3. NLnet: Filtrera bort navigeringslänkar från resultaten. Säkerställ att 
   varje hämtat "bidrag" innehåller: titel, deadline, belopp och direkt URL 
   till ansökningssidan. Målet är 3-5 rena programposter (Commons Fund, 
   TALER, Fediversity, NGI Zero Review).

4. EDF deduplicering: Verifiera att EDF-wrapper:n inte skapar dubbletter 
   med EU F&T-scraper:n. Lägg till explicit URL-baserad dedup-kontroll.

5. Konsistens: Sätt "37 sources" och "1,460+ grants" konsistent i ALLA 
   filer: landing page, translations (sv + en), statsbar, replit.md.

6. Scraper health: Markera EIT Health och EIT Digital som "url_error" i 
   scraper_sources-tabellen (inte "active") tills korrekta URLs är 
   verifierade.

Rapportera: antal bidrag per källa efter fix, total bidragssum, och 
vilka URLs som nu används för varje EIT KIC.
Sammanfattning av vad som gick bra
EDF (27), NATO SPS (24) och COST Actions (10) fungerar väl och levererade vettiga antal. Nordic Innovation (10) och CASSINI (5) är rimliga givet källornas storlek. Arkitekturen med BaseScraper och dispatch-routing är korrekt implementerad. Databasen är uppdaterad.

Viktigaste åtgärden: EIT KICs bör ge 30+ bidrag med Playwright – det är den enskilt största möjligheten att förbättra resultaten snabbt.

skicka gärna ett uppdaterat replit prompt på engelska för detta: Följande problem behöver åtgärdas i de nya scrapers:
1. EIT KICs (kritiskt): 7 av 9 KICs returnerar 0 bidrag pga JS-rendering.
   Byt till Playwright för följande KICs:
   - EIT Health (eithealth.eu)
   - EIT Digital (eitdigital.eu)
   - EIT Manufacturing (eitmanufacturing.eu)
   - EIT RawMaterials (eitrawmaterials.eu)
   - EIT InnoEnergy (eitinnoenergy.com)
   - EIT Culture & Creativity (eitculture.eu)
   - EIT Climate-KIC (climate-kic.org)

   Hämta och verifiera korrekt URL för calls/open-calls-sidan på varje KIC.
   Målet är minst 3-5 bidrag per KIC, totalt 30+ för alla 9.
2. IHI (kritiskt): Scraper:n faller tillbaka på hårdkodade "known programs" 
   istället för att hämta live data. Fixa SSL-problemet (acceptera certifikat 
   eller använd requests med verify=False + varning i log) och hämta aktiva 
   calls från https://www.ihi.europa.eu/apply-funding/open-calls
   IHI Call 12 är aktiv nu med deadline i april 2026.
3. NLnet: Filtrera bort navigeringslänkar från resultaten. Säkerställ att 
   varje hämtat "bidrag" innehåller: titel, deadline, belopp och direkt URL 
   till ansökningssidan. Målet är 3-5 rena programposter (Commons Fund, 
   TALER, Fediversity, NGI Zero Review).
4. EDF deduplicering: Verifiera att EDF-wrapper:n inte skapar dubbletter 
   med EU F&T-scraper:n. Lägg till explicit URL-baserad dedup-kontroll.
5. Konsistens: Sätt "37 sources" och "1,460+ grants" konsistent i ALLA 
   filer: landing page, translations (sv + en), statsbar, replit.md.
6. Scraper health: Markera EIT Health och EIT Digital som "url_error" i 
   scraper_sources-tabellen (inte "active") tills korrekta URLs är 
   verifierade.
Rapportera: antal bidrag per källa efter fix, total bidragssum, och 
vilka URLs som nu används för varje EIT KIC.

Show more
17:27

Searched the web
Searched the web

Here's the Replit prompt:

Fix 8 New Scrapers — Quality Pass

Several issues were found after the initial implementation. Fix them in order of priority.

🔴 CRITICAL FIX 1: EIT KICs — Switch to Playwright

7 of 9 EIT KICs return 0 grants because they use JS-rendered pages. BeautifulSoup won't work here.

For each KIC below, do the following steps before writing any code:

Open the KIC's website and manually navigate to their calls/open-calls page
Confirm the correct URL is live and accessible
Inspect the HTML structure to find grant listing selectors
Only then update the scraper to use Playwright
KICs to fix:

KIC	Base URL	Try these paths first
EIT Health	eithealth.eu	/new-call-opportunities/ or /opportunities/
EIT Digital	eitdigital.eu	/funding/ or /open-calls/
EIT Manufacturing	eitmanufacturing.eu	/open-calls/ or /apply/
EIT RawMaterials	eitrawmaterials.eu	/funding/ or /open-calls/
EIT InnoEnergy	eitinnoenergy.com	/open-calls/ or /funding/
EIT Culture & Creativity	eitculture.eu	/open-calls/ or /funding/
EIT Climate-KIC	climate-kic.org	/programmes/ or /open-calls/
Implementation requirements:

Use the existing Playwright pattern from tillvaxtverket.py or region_scraper.py as your template
Add await page.wait_for_load_state('networkidle') before scraping
If a KIC has no currently open calls, fall back to scraping "upcoming" or "all calls" — do NOT return 0, instead store the programmes with status: "upcoming"
Target: minimum 3–5 grants per KIC, 30+ total across all 9
Important note on EIT Health specifically: EIT Health's Flagship Calls page (eithealth.eu/our-flagships/) currently states "no open calls" — this is expected. Instead scrape eithealth.eu/new-call-opportunities/ which lists THI (Transformative Health Instrument) and other active programmes. Confirmed live URL: https://eithealth.eu/new-call-opportunities/

🔴 CRITICAL FIX 2: IHI — Fix SSL and remove hardcoded fallback

The IHI scraper currently ignores the live website and falls back to hardcoded "known programs". This is wrong — it means we'll never detect new calls or deadlines.

Fix:

python
# Replace the current SSL-failing request with:
import urllib3
urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)
response = requests.get(url, verify=False, timeout=30)
logger.warning("IHI: SSL verification disabled — known issue with ihi.europa.eu cert")
Then scrape the actual live page: https://www.ihi.europa.eu/apply-funding/open-calls

IHI Call 12 is currently active with deadline around April 2026. It should appear in results after this fix. Remove the hardcoded fallback entirely — if the live page fails, log an error and return empty rather than returning stale hardcoded data.

Expected output after fix: 3–8 grants (IHI typically has 2–4 open calls at any time plus upcoming calls).

🟠 FIX 3: NLnet — Filter navigation links

The NLnet scraper is capturing navigation links and page furniture as "grants".

Fix the selectors so that only actual funding programmes are returned. Each grant must have ALL of these fields populated to be included:

title — actual programme name (e.g. "NGI Zero Commons Fund", "NGI TALER", "NGI Fediversity")
url — direct link to the programme page (not a nav link, must contain /project/ or /programmes/ or /funds/)
description — at least 50 characters
amount — if not found, set to null (don't block the record, but don't guess)
deadline — if not found, check the programme page for next submission window
Target: 3–5 clean programme records. The core NLnet funds to expect: Commons Fund, TALER, Fediversity, NGI Zero Review, Zero Core.

Filter logic to add:

python
# Exclude if URL contains any of these patterns:
EXCLUDE_PATTERNS = ['#', 'mailto:', '/news/', '/about/', '/contact/', '/team/', '/blog/']
# Require minimum description length:
if len(description) < 50:
    continue
🟡 FIX 4: EDF — Prevent duplicates with EU F&T scraper

The EDF scraper is a wrapper around the EU F&T scraper using programme ID 44181033. This creates a risk of the same grants appearing twice in the database — once from the full EU F&T run and once from the EDF wrapper.

Add this deduplication check in the EDF scraper:

python
# In edf_scraper.py, after fetching grants, before upserting:
def deduplicate_against_eu_ft(self, grants: list) -> list:
    """Remove grants already stored by the EU F&T scraper to prevent duplicates."""
    existing_urls = self.db.get_grant_urls_by_source('eu_funding_tenders')
    deduped = [g for g in grants if g['source_url'] not in existing_urls]
    logger.info(f"EDF: {len(grants)} fetched, {len(deduped)} after dedup against EU F&T")
    return deduped
If get_grant_urls_by_source doesn't exist yet, add it to db.py:

python
def get_grant_urls_by_source(self, source_key: str) -> set:
    result = self.db.execute(
        "SELECT source_url FROM grants WHERE source = $1", [source_key]
    )
    return {row['source_url'] for row in result}
🟡 FIX 5: Consistency — Update all count references

After the fixes above are complete and you have verified the new grant totals, update ALL of the following files to show consistent numbers. Use the actual post-fix count, but floor to a round number (e.g. if total is 1,487 → use "1,480+" or "1,500+" — pick one and use it everywhere):

Files to update:

client/src/pages/landing.tsx — hero section, stats bar
client/src/i18n/sv.json — all grant/source count strings
client/src/i18n/en.json — all grant/source count strings
replit.md — overview section, current state description
Any other .tsx, .ts, or .json file containing hardcoded grant/source counts
Search for all occurrences of: "1,4", "1,3", "27 sources", "35+", "37" to find everything that needs updating.

Source count: use 37 (or the actual registered count from scraper_sources table — run SELECT COUNT(*) FROM scraper_sources WHERE status = 'active' to verify).

🟡 FIX 6: Scraper health — Mark broken EIT scrapers correctly

Until EIT Health and EIT Digital have confirmed working URLs, mark them as non-active in the database so the health monitoring dashboard doesn't show false positives:

sql
UPDATE scraper_sources 
SET status = 'url_error', 
    error_message = 'Original URL returned 404 — needs URL verification before re-enabling',
    last_error_at = NOW()
WHERE source_key IN ('eit_health', 'eit_digital');
```

Once you have found and confirmed working URLs for these two in Fix 1 above, update them back to `active` at that point.

---

**Testing protocol**

After all fixes, run each scraper individually and report results in this format:
```
SOURCE                  | GRANTS FOUND | URL USED                          | STATUS
------------------------|-------------|-----------------------------------|--------
EIT Health              | X           | https://...                       | ✅/❌
EIT Digital             | X           | https://...                       | ✅/❌
EIT Manufacturing       | X           | https://...                       | ✅/❌
EIT RawMaterials        | X           | https://...                       | ✅/❌
EIT InnoEnergy          | X           | https://...                       | ✅/❌
EIT Culture & Creativity| X           | https://...                       | ✅/❌
EIT Climate-KIC         | X           | https://...                       | ✅/❌
EIT Food                | X           | https://...                       | ✅/❌
EIT Urban Mobility      | X           | https://...                       | ✅/❌
IHI                     | X           | https://...                       | ✅/❌
NLnet                   | X           | https://...                       | ✅/❌
EDF                     | X (deduped) | EU F&T wrapper, prog ID 44181033  | ✅/❌
------------------------|-------------|-----------------------------------|--------
TOTAL NEW FROM FIXES    | X           |                                   |
TOTAL GRANTS IN DB      | X           |                                   |
ACTIVE SOURCES IN DB    | X           |                                   |
Success criteria: EIT KICs total ≥ 30 grants, IHI ≥ 3 grants, NLnet = 3–5 clean records, zero EDF duplicates, all count references consistent across codebase.

PrioritetFunktionInspiration frånMotivering🔴 KritiskAI-ansökningsskrivningGrantPilot.se, Cogrant Write, GrantableUtan detta förlorar GetGrant.ai halva värdekedjan till GrantPilot.se. Stöd för Vinnova/Tillväxtverkets mallar och bedömningskriterier🔴 KritiskEligibility-check med kan du hjälpa mig att skriva detaljerade replit prompts på engelska för att addera följande som du kom frma till din AIGranter.ai, InstrumentlAutomatisk kontroll om företaget uppfyller behörighetskraven innan tid investeras i ansökan Granter🟠 Hög"Varför matchar detta?" – AI-förklaringInstrumentl AI ProspectingInstrumentl förklarar varför varje funder matchar. Sparkthefiregrantwriting Bygg förtroende genom transparens Sparkthefiregrantwriting🟠 HögDeadline-kalender + push-notiserInstrumentl, GrantHubProaktiva påminnelser om kommande deadlines för matchade bidrag🟡 MediumTeam-samarbete i ansökningarGrantPilot.se, Cogrant, InstrumentlBjud in kollegor, realtidsredigering, grantpilotGrantpilot kommentarsfunktion🟡 MediumCompliance-check mot bidragskriterierCogrant Write, Granter.aiAI granskar utkast mot officiella bedömningskriterier Granter och identifierar gap CograntGranter🟡 MediumSmart Content LibraryGrantableLär sig från tidigare ansökningar och organisationsdata för att skriva i rätt ton OpenGrantsGrantable🟢 FramtidPost-award projekthanteringGranter.ai, BidragsportalenMilstolpstrack, rapportering, compliance efter godkännande Granter🟢 FramtidSuccess fee-modell (tillval)Granter.ai2–5 % vid framgångsrik ansökan EliteAI – sänker tröskel för nya kunder🟢 FramtidPeer insights / benchmarkingInstrumentl Peer Prospecting"Företag som liknar ditt har fått bidrag från X"🟢 FramtidWhite-label för konsulterGranter.aiBidragskonsulter som kanal – de märker plattformen med sitt varumärke Granter

Show more
17:34
Här är detaljerade Replit-prompts för varje funktion, i prioritetsordning:

🔴 CRITICAL 1: AI Application Writing Module
Build an AI-powered grant application writing module for GetGrant.ai.

## Overview
Users select a grant, answer a few questions about their project, and the AI generates 
a complete draft application tailored to that specific grant's requirements and 
evaluation criteria.

## Backend: New API endpoints

Create `server/routes/applications.ts` with these endpoints:

### POST /api/applications/generate
Generate a draft application for a specific grant.

Request body:
{
  grantId: string,
  companyProfile: {
    name: string,
    description: string,
    sector: string,
    employees: number,
    revenue?: number,
    founded?: number,
    region: string
  },
  projectDescription: string,      // 200-1000 chars, what they want to do
  projectGoals: string,             // expected outcomes
  projectBudget: number,            // total project budget in SEK
  requestedAmount: number,          // amount applying for
  previousExperience?: string       // relevant prior work
}

Response:
{
  applicationId: string,
  grantId: string,
  grantTitle: string,
  sections: [
    {
      sectionKey: string,           // e.g. "project_description"
      sectionTitle: string,         // e.g. "Projektbeskrivning"
      content: string,              // AI-generated text
      wordCount: number,
      maxWords?: number,            // from grant template if known
      evaluationCriteria?: string   // what judges look for in this section
    }
  ],
  overallScore?: number,            // estimated fit score 0-100
  warnings: string[],               // e.g. "Budget exceeds maximum grant amount"
  generatedAt: string
}

### GET /api/applications/:id
Retrieve a saved application draft.

### PUT /api/applications/:id/section/:sectionKey
Update a specific section (user edits).

Request body: { content: string }

### POST /api/applications/:id/regenerate-section
Regenerate a specific section with new instructions.

Request body: { sectionKey: string, instructions: string }

### GET /api/applications/templates/:grantSource
Get the application template for a specific grant source.

## Database schema

Add to schema.ts:
```typescript
export const applications = pgTable('applications', {
  id: uuid('id').primaryKey().defaultRandom(),
  userId: varchar('user_id').notNull(),
  grantId: varchar('grant_id').notNull(),
  status: varchar('status').default('draft'), // draft | submitted | archived
  sections: jsonb('sections').notNull(),
  companySnapshot: jsonb('company_snapshot').notNull(),
  projectData: jsonb('project_data').notNull(),
  overallScore: integer('overall_score'),
  warnings: jsonb('warnings').default('[]'),
  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
});
```

## AI Service: server/services/applicationWriter.ts
```typescript
import Anthropic from '@anthropic-ai/sdk';

const GRANT_TEMPLATES: Record<string, GrantTemplate> = {
  vinnova: {
    sections: [
      {
        key: 'background_and_need',
        title: 'Bakgrund och behov',
        description: 'Beskriv problemet eller behovet som projektet adresserar',
        maxWords: 500,
        evaluationCriteria: 'Relevance to Swedish innovation needs, clarity of problem statement, market size'
      },
      {
        key: 'project_description',
        title: 'Projektbeskrivning',
        description: 'Beskriv vad projektet ska göra och hur',
        maxWords: 800,
        evaluationCriteria: 'Technical feasibility, innovation level (TRL), methodology'
      },
      {
        key: 'expected_results',
        title: 'Förväntade resultat och effekter',
        description: 'Vilka konkreta resultat förväntas och vilken samhällsnytta skapas',
        maxWords: 400,
        evaluationCriteria: 'Measurable outcomes, societal impact, commercialization potential'
      },
      {
        key: 'team_and_capacity',
        title: 'Projektparter och kapacitet',
        description: 'Beskriv teamet och deras relevanta erfarenhet',
        maxWords: 300,
        evaluationCriteria: 'Team competence, collaboration, relevant track record'
      },
      {
        key: 'budget_motivation',
        title: 'Budget och motivering',
        description: 'Specificera kostnader och motivera dem',
        maxWords: 300,
        evaluationCriteria: 'Cost efficiency, realistic budget, co-financing'
      }
    ]
  },
  tillvaxtverket: {
    sections: [
      {
        key: 'company_description',
        title: 'Företagsbeskrivning',
        description: 'Beskriv företaget, dess verksamhet och marknadsposition',
        maxWords: 400,
        evaluationCriteria: 'Company maturity, market validation, growth potential'
      },
      {
        key: 'project_purpose',
        title: 'Projektets syfte och mål',
        description: 'Vad ska projektet uppnå och varför är det viktigt',
        maxWords: 500,
        evaluationCriteria: 'Alignment with regional growth goals, job creation potential'
      },
      {
        key: 'implementation_plan',
        title: 'Genomförandeplan',
        description: 'Hur ska projektet genomföras, tidplan och milstolpar',
        maxWords: 600,
        evaluationCriteria: 'Realistic timeline, clear milestones, risk management'
      },
      {
        key: 'sustainability',
        title: 'Hållbarhet och långsiktighet',
        description: 'Hur säkras resultaten efter projektets slut',
        maxWords: 300,
        evaluationCriteria: 'Long-term viability, environmental sustainability'
      }
    ]
  },
  generic: {
    sections: [
      {
        key: 'executive_summary',
        title: 'Executive Summary',
        description: 'Brief overview of the project and funding request',
        maxWords: 300,
        evaluationCriteria: 'Clarity, compelling case, key differentiators'
      },
      {
        key: 'problem_and_solution',
        title: 'Problem & Solution',
        description: 'What problem does this solve and how',
        maxWords: 600,
        evaluationCriteria: 'Problem validation, solution uniqueness, market fit'
      },
      {
        key: 'impact',
        title: 'Expected Impact',
        description: 'Measurable outcomes and broader societal value',
        maxWords: 400,
        evaluationCriteria: 'Quantified impact, sustainability, scalability'
      },
      {
        key: 'team',
        title: 'Team',
        description: 'Why this team can deliver',
        maxWords: 300,
        evaluationCriteria: 'Relevant expertise, track record, commitment'
      },
      {
        key: 'budget',
        title: 'Budget Overview',
        description: 'How funds will be used',
        maxWords: 300,
        evaluationCriteria: 'Efficiency, transparency, leverage of grant funds'
      }
    ]
  }
};

export async function generateApplication(
  grant: Grant,
  companyProfile: CompanyProfile,
  projectData: ProjectData
): Promise<GeneratedApplication> {
  const client = new Anthropic();
  
  // Determine which template to use based on grant source
  const sourceKey = grant.source.toLowerCase();
  const template = GRANT_TEMPLATES[sourceKey] || GRANT_TEMPLATES.generic;
  
  const sections = [];
  
  // Generate each section with a targeted prompt
  for (const sectionTemplate of template.sections) {
    const prompt = buildSectionPrompt(
      grant, 
      companyProfile, 
      projectData, 
      sectionTemplate
    );
    
    const response = await client.messages.create({
      model: 'claude-opus-4-5',
      max_tokens: 1500,
      messages: [{ role: 'user', content: prompt }]
    });
    
    const content = response.content[0].type === 'text' 
      ? response.content[0].text 
      : '';
    
    sections.push({
      sectionKey: sectionTemplate.key,
      sectionTitle: sectionTemplate.title,
      content: content.trim(),
      wordCount: content.split(' ').length,
      maxWords: sectionTemplate.maxWords,
      evaluationCriteria: sectionTemplate.evaluationCriteria
    });
  }
  
  // Generate overall warnings
  const warnings = generateWarnings(grant, companyProfile, projectData);
  
  return { sections, warnings };
}

function buildSectionPrompt(
  grant: Grant,
  company: CompanyProfile, 
  project: ProjectData,
  section: SectionTemplate
): string {
  return `You are an expert Swedish grant application writer. Write the "${section.title}" 
section for a grant application.

GRANT INFORMATION:
- Grant: ${grant.title}
- Funder: ${grant.source}
- Grant description: ${grant.description}
- Max amount: ${grant.maxAmount ? `${grant.maxAmount} SEK` : 'Not specified'}
- Deadline: ${grant.deadline || 'Not specified'}

COMPANY INFORMATION:
- Company: ${company.name}
- Sector: ${company.sector}
- Description: ${company.description}
- Employees: ${company.employees}
- Region: ${company.region}
${company.revenue ? `- Annual revenue: ${company.revenue} SEK` : ''}

PROJECT INFORMATION:
- Project description: ${project.projectDescription}
- Goals: ${project.projectGoals}
- Total budget: ${project.projectBudget} SEK
- Requested amount: ${project.requestedAmount} SEK
${project.previousExperience ? `- Relevant experience: ${project.previousExperience}` : ''}

SECTION INSTRUCTIONS:
- Section to write: ${section.title}
- What this section should cover: ${section.description}
- Max length: ${section.maxWords || 500} words
- Evaluation criteria (what judges look for): ${section.evaluationCriteria}

WRITING REQUIREMENTS:
- Write in Swedish if the grant is from a Swedish agency (Vinnova, Tillväxtverket, 
  Energimyndigheten, etc.), otherwise write in English
- Be specific and use concrete numbers where possible
- Align the language with what grant evaluators want to see
- Do NOT use generic filler phrases — every sentence should add value
- Do NOT exceed the word limit
- Write in first person plural ("Vi planerar..." / "Our project will...")
- Output ONLY the section text, no headers or meta-commentary

Write the section now:`;
}
```

## Frontend: New page client/src/pages/apply.tsx

Create a 4-step wizard:

**Step 1 — Select grant**
- Show the grant the user clicked "Apply" from (pre-selected)
- Display grant summary: title, funder, deadline, max amount
- Show match score and why it matched (pulls from existing matching data)

**Step 2 — Company profile**
- Pre-fill from user's saved profile if available
- Fields: company name, description (textarea), sector (dropdown using 
  existing sector list), employees (number), annual revenue (optional), 
  region (dropdown), founded year (optional)
- "Save to profile" checkbox

**Step 3 — Project information**
- Project description (textarea, 200-1000 chars, live character count)
- Project goals (textarea, what will be achieved)
- Total project budget (SEK, number input)
- Requested amount (SEK, auto-validates against grant max)
- Previous relevant experience (optional textarea)

**Step 4 — Generated application**
- Show loading state: "Generating your application... this takes about 30 seconds"
- Animated progress with messages like "Analyzing grant criteria...", 
  "Writing project description...", "Reviewing for compliance..."
- Once generated, show each section as an editable card:
  - Section title
  - Word count / max words indicator (green if under, red if over)
  - Evaluation criteria shown as subtle hint text
  - Editable textarea with the AI content
  - "Regenerate this section" button (opens modal for custom instructions)
- Warnings banner at top if any (e.g. "Your requested amount exceeds the grant maximum")
- Bottom bar: "Copy all", "Download as PDF", "Download as Word (.docx)"

## Export functionality

Add `server/services/exportApplication.ts`:

**PDF export**: Use `pdfkit` npm package to generate a formatted PDF
**Word export**: Use `docx` npm package to generate a .docx file

Both exports should include:
- Header: Grant name, company name, generated date
- Each section with its title as a heading
- Page numbers
- Footer: "Generated by GetGrant.ai"

## Navigation integration

Add "Apply with AI" button to:
1. Grant detail page (`/grants/:id`) — primary CTA button
2. Bookmarked grants list
3. Match results — "Apply" action next to each match

Route: `/apply?grantId=:id`

## Database migration

Run: `npm run db:push` after adding the applications table to schema.ts

## Testing checklist
- [ ] Generate application for a Vinnova grant — verify Swedish output
- [ ] Generate application for an EU grant — verify English output  
- [ ] Section regeneration with custom instructions works
- [ ] Word count limits enforced in UI
- [ ] PDF export downloads correctly
- [ ] Word export downloads correctly
- [ ] Warnings appear when requested amount > grant maximum
- [ ] Company profile saves and pre-fills on next use
- [ ] Application saves to database and can be retrieved
🔴 CRITICAL 2: Eligibility Check
Build an AI-powered eligibility pre-check feature that automatically verifies 
whether a company meets a grant's requirements before they invest time applying.

## Overview
When a user views a grant or starts an application, they see an instant eligibility 
verdict: ✅ Likely eligible / ⚠️ Partially eligible / ❌ Likely ineligible — with 
clear explanations for each criterion.

## Backend: server/services/eligibilityChecker.ts
```typescript
import Anthropic from '@anthropic-ai/sdk';

interface EligibilityResult {
  verdict: 'eligible' | 'partial' | 'ineligible' | 'unknown';
  score: number;                    // 0-100 confidence
  criteria: EligibilityCriterion[];
  summary: string;                  // 1-2 sentence plain language verdict
  blockers: string[];               // hard blockers that disqualify
  warnings: string[];               // soft issues that may reduce chances
  strengths: string[];              // where the company is a strong fit
}

interface EligibilityCriterion {
  name: string;                     // e.g. "Company size"
  requirement: string;              // e.g. "Max 250 employees (SME)"
  companyValue: string;             // e.g. "45 employees"
  status: 'pass' | 'fail' | 'warning' | 'unknown';
  explanation: string;
}

export async function checkEligibility(
  grant: Grant,
  companyProfile: CompanyProfile
): Promise<EligibilityResult> {
  const client = new Anthropic();
  
  // First try structured eligibility data if available
  if (grant.eligibilityData) {
    const structuredResult = checkStructuredEligibility(
      grant.eligibilityData, 
      companyProfile
    );
    // If we got a confident result from structured data, enrich with AI summary
    if (structuredResult.score > 80 || structuredResult.blockers.length > 0) {
      structuredResult.summary = await generateEligibilitySummary(
        structuredResult, grant, companyProfile
      );
      return structuredResult;
    }
  }
  
  // Fall back to AI analysis of raw grant text
  const prompt = buildEligibilityPrompt(grant, companyProfile);
  
  const response = await client.messages.create({
    model: 'claude-opus-4-5',
    max_tokens: 1000,
    messages: [{ role: 'user', content: prompt }],
    system: `You are a Swedish grant eligibility expert. Analyze whether a company 
    meets a grant's requirements. Always respond with valid JSON only, no prose.`
  });
  
  const text = response.content[0].type === 'text' ? response.content[0].text : '{}';
  
  try {
    return JSON.parse(text) as EligibilityResult;
  } catch {
    return { verdict: 'unknown', score: 0, criteria: [], 
             summary: 'Could not determine eligibility', 
             blockers: [], warnings: [], strengths: [] };
  }
}

function checkStructuredEligibility(
  eligibilityData: GrantEligibilityData,
  company: CompanyProfile
): EligibilityResult {
  const criteria: EligibilityCriterion[] = [];
  const blockers: string[] = [];
  const warnings: string[] = [];
  const strengths: string[] = [];
  
  // Company size check
  if (eligibilityData.maxEmployees) {
    const pass = company.employees <= eligibilityData.maxEmployees;
    criteria.push({
      name: 'Company size',
      requirement: `Max ${eligibilityData.maxEmployees} employees`,
      companyValue: `${company.employees} employees`,
      status: pass ? 'pass' : 'fail',
      explanation: pass 
        ? `Your company (${company.employees} employees) is within the limit`
        : `Your company exceeds the maximum size requirement`
    });
    if (!pass) blockers.push(`Company too large (${company.employees} vs max ${eligibilityData.maxEmployees})`);
    else strengths.push('Company size qualifies as SME');
  }
  
  // Geography check
  if (eligibilityData.geographyRequirements?.length) {
    const geoMatch = eligibilityData.geographyRequirements.some(req => 
      req.toLowerCase().includes(company.region?.toLowerCase() || '') ||
      req.toLowerCase().includes('sweden') ||
      req.toLowerCase().includes('sverige')
    );
    criteria.push({
      name: 'Geographic eligibility',
      requirement: eligibilityData.geographyRequirements.join(', '),
      companyValue: company.region || 'Not specified',
      status: geoMatch ? 'pass' : 'warning',
      explanation: geoMatch 
        ? 'Your region appears to be covered'
        : 'Verify your region is covered by this grant'
    });
  }
  
  // Company type check
  if (eligibilityData.companyTypes?.length) {
    const typeMatch = eligibilityData.companyTypes.some(t => 
      t.toLowerCase().includes(company.companyType?.toLowerCase() || 'company')
    );
    criteria.push({
      name: 'Organization type',
      requirement: eligibilityData.companyTypes.join(' or '),
      companyValue: company.companyType || 'Company',
      status: typeMatch ? 'pass' : 'warning',
      explanation: typeMatch ? 'Your organization type is eligible' : 'Verify your organization type is covered'
    });
  }
  
  // Sector check
  if (eligibilityData.sectors?.length) {
    const sectorMatch = eligibilityData.sectors.some(s =>
      s.toLowerCase().includes(company.sector?.toLowerCase() || '')
    );
    if (sectorMatch) strengths.push(`Your sector (${company.sector}) is explicitly targeted`);
  }
  
  const failCount = criteria.filter(c => c.status === 'fail').length;
  const passCount = criteria.filter(c => c.status === 'pass').length;
  
  let verdict: EligibilityResult['verdict'];
  let score: number;
  
  if (failCount > 0) {
    verdict = 'ineligible';
    score = Math.max(0, 30 - (failCount * 15));
  } else if (criteria.length === 0) {
    verdict = 'unknown';
    score = 50;
  } else if (passCount === criteria.length) {
    verdict = 'eligible';
    score = 85 + Math.min(15, strengths.length * 5);
  } else {
    verdict = 'partial';
    score = 60;
  }
  
  return { verdict, score, criteria, summary: '', blockers, warnings, strengths };
}

function buildEligibilityPrompt(grant: Grant, company: CompanyProfile): string {
  return `Analyze whether this company is eligible for this grant.

GRANT:
Title: ${grant.title}
Source: ${grant.source}
Description: ${grant.description}
Eligibility text: ${grant.eligibilityText || 'Not available — infer from description'}
Max amount: ${grant.maxAmount || 'Not specified'}

COMPANY:
Name: ${company.name}
Sector: ${company.sector}
Employees: ${company.employees}
Annual revenue: ${company.revenue || 'Not provided'}
Region: ${company.region}
Company type: ${company.companyType || 'Private company (AB)'}
Founded: ${company.founded || 'Not provided'}
Description: ${company.description}

Respond with JSON only in this exact structure:
{
  "verdict": "eligible" | "partial" | "ineligible" | "unknown",
  "score": 0-100,
  "criteria": [
    {
      "name": "criterion name",
      "requirement": "what the grant requires",
      "companyValue": "company's actual value",
      "status": "pass" | "fail" | "warning" | "unknown",
      "explanation": "1 sentence explanation"
    }
  ],
  "summary": "1-2 sentence plain Swedish/English verdict",
  "blockers": ["hard disqualifier 1"],
  "warnings": ["soft concern 1"],
  "strengths": ["strong match point 1"]
}`;
}
```

## New API endpoint

Add to `server/routes/grants.ts`:
POST /api/grants/:id/eligibility-check Body: { companyProfile: CompanyProfile } Response: EligibilityResult


Cache results for 24 hours per grant+company combination to avoid redundant AI calls:
```typescript
const cacheKey = `eligibility:${grantId}:${userId}`;
// Use existing cache mechanism or simple in-memory Map with TTL
```

## Frontend component: EligibilityBadge

Create `client/src/components/EligibilityBadge.tsx`:

**Small inline variant** (for grant cards in search results):
[✅ Eligible] [⚠️ Check criteria] [❌ Not eligible]

Click opens the full panel.

**Full panel variant** (in grant detail page, before "Apply" button):
┌─────────────────────────────────────────────────────┐
│ 🎯 Eligibility Check                                 │
│                                                      │
│  ✅ Likely Eligible  •  Score: 87/100               │
│                                                      │
│  Based on your company profile:                      │
│  ✅ Company size: 45 employees (max 250) — PASS     │
│  ✅ Region: Stockholm — PASS                         │
│  ✅ Sector: CleanTech — targeted sector              │
│  ⚠️ Co-funding: 30% required — verify you have this │
│                                                      │
│  💪 Strengths: Your sector is explicitly targeted    │
│                                                      │
│  "Your company meets all hard requirements for      │
│   this grant. Confirm you can provide 30%           │
│   co-funding before applying."                       │
│                                                      │
│  [Update my profile]  [Proceed to Application →]    │
└─────────────────────────────────────────────────────┘


**Loading state**: Show spinner with "Checking eligibility..." for 2-3 seconds.

**"Profile incomplete" state**: If user hasn't filled company profile, show:
"Complete your company profile to get an instant eligibility check"
[Complete profile →]


## Integration points

1. **Grant detail page** (`/grants/:id`): Show full EligibilityBadge panel above "Apply" button. Auto-run check when page loads if user is logged in and has profile.

2. **Search results** (`/search`): Show small inline EligibilityBadge on each grant card. Run checks lazily (only for visible cards, use Intersection Observer).

3. **AI Match results**: Include eligibility verdict in each match result card.

4. **Apply wizard Step 1**: Re-show eligibility check. If ineligible, show warning but don't block — let user proceed with informed consent ("This grant has requirements you may not meet. Continue anyway?").

## Testing checklist
- [ ] Eligible company gets ✅ verdict with score >75
- [ ] Company with wrong size gets ❌ with clear blocker message
- [ ] Partial match (missing some info) gets ⚠️ with specific warnings
- [ ] Check runs automatically on grant detail page for logged-in users
- [ ] Results cached — second view doesn't call AI again
- [ ] "Update profile" link navigates to profile page
- [ ] Works on mobile (panel collapses gracefully on small screens)
- [ ] Ineligible verdict still allows proceeding to application
🟠 HIGH 1: "Why does this match?" AI Explanation
Add transparent AI match explanations to every grant match result, showing users 
exactly why a grant was recommended for them.

## Context
Currently the AI matching returns a score but no explanation. Users don't know 
why a grant matched, which reduces trust and engagement. This adds a "Why this 
match?" feature inspired by Instrumentl's AI Prospecting Assistant.

## Backend changes

### Update the matching service

In `server/services/matching.ts` (or wherever AI matching runs), add explanation 
generation to the existing match response.

Add this function:
```typescript
export async function generateMatchExplanation(
  grant: Grant,
  companyProfile: CompanyProfile,
  matchScore: number
): Promise<MatchExplanation> {
  const client = new Anthropic();
  
  const response = await client.messages.create({
    model: 'claude-haiku-4-5',  // Use Haiku for cost efficiency — this runs per grant
    max_tokens: 300,
    messages: [{
      role: 'user',
      content: `Explain in 2-3 bullet points why this grant matches this company.
Be specific — reference actual details from both. Be direct and concrete.
Write in the same language as the grant (Swedish for Swedish grants, English for EU grants).

GRANT: ${grant.title}
FUNDER: ${grant.source}  
GRANT FOCUS: ${grant.description?.substring(0, 300)}
SECTORS TARGETED: ${grant.sectors?.join(', ') || 'Not specified'}

COMPANY: ${companyProfile.name}
SECTOR: ${companyProfile.sector}
DESCRIPTION: ${companyProfile.description?.substring(0, 200)}
REGION: ${companyProfile.region}

MATCH SCORE: ${matchScore}/100

Respond with JSON:
{
  "headline": "One sentence summary of the match (max 15 words)",
  "reasons": [
    "Specific reason 1 — reference actual grant criteria and company detail",
    "Specific reason 2",
    "Specific reason 3 (optional)"
  ],
  "bestFitAspect": "The single strongest alignment point in 8 words or less"
}`
    }]
  });
  
  const text = response.content[0].type === 'text' ? response.content[0].text : '{}';
  try {
    return JSON.parse(text);
  } catch {
    return {
      headline: `Strong match for ${companyProfile.sector} companies`,
      reasons: ['Grant focus aligns with your sector', 'Your region is covered'],
      bestFitAspect: 'Sector alignment'
    };
  }
}
```

### Update match API response

Add `explanation` field to match results:
```typescript
// In the match results endpoint, add explanation to top N matches
// Only generate explanations for top 10 matches to control API costs

const matchesWithExplanations = await Promise.all(
  topMatches.slice(0, 10).map(async (match) => ({
    ...match,
    explanation: await generateMatchExplanation(
      match.grant, 
      companyProfile, 
      match.score
    )
  }))
);

// Remaining matches (11-50) get explanation: null — shown on demand
```

### New endpoint for on-demand explanation
POST /api/grants/:id/explain-match Body: { companyProfile: CompanyProfile } Response: { explanation: MatchExplanation }


Cache explanations per grant+user for the session.

## Frontend: MatchExplanation component

Create `client/src/components/MatchExplanation.tsx`:
```tsx
// Collapsed state (default on grant cards):
<div className="match-explanation-collapsed">
  <span className="match-score">87% match</span>
  <button onClick={() => setExpanded(true)}>
    Why? <ChevronDown size={14} />
  </button>
</div>

// Expanded state:
<div className="match-explanation-expanded">
  <div className="explanation-headline">
    {explanation.headline}
  </div>
  <ul className="explanation-reasons">
    {explanation.reasons.map(reason => (
      <li key={reason}>
        <CheckCircle size={14} className="text-green-500" />
        {reason}
      </li>
    ))}
  </ul>
  <div className="best-fit-badge">
    ⭐ Best fit: {explanation.bestFitAspect}
  </div>
</div>
```

**Loading state**: When explanation is null and user clicks "Why?", show spinner 
for 1-2 seconds while fetching on-demand.

## Visual design requirements

The explanation should feel like a confident advisor, not a disclaimer:

- Use green checkmarks (✅) for reasons, not bullets
- Headline in slightly larger, bolder text
- "Best fit aspect" as a small badge/pill with star icon
- Smooth expand/collapse animation (max-height transition)
- On mobile: expand inline below the grant card, don't use modal

## Integration

Add MatchExplanation to:
1. AI Match results page — next to each match score
2. Grant detail page — in the "Why we recommend this" section (if coming from match)
3. Bookmarks — show match explanation for saved grants

## Cost optimization
- Use `claude-haiku-4-5` model only (5-10x cheaper than Opus for this task)  
- Generate for top 10 matches automatically, rest on-demand
- Cache in localStorage for current session: `matchExplanation:${grantId}:${userId}`
- Skip generation if match score < 40 (low matches don't need explanation)

## Testing checklist
- [ ] Explanation appears for top 10 match results automatically
- [ ] "Why?" button works for matches 11-50, fetches on demand
- [ ] Explanations reference actual company and grant data (not generic)
- [ ] Swedish grants generate Swedish explanations
- [ ] EU grants generate English explanations
- [ ] Loading state shows during on-demand fetch
- [ ] Cached explanations load instantly on second view
- [ ] Mobile layout works correctly
🟠 HIGH 2: Deadline Calendar + Push Notifications
Build a deadline calendar that shows upcoming deadlines for grants the user has 
bookmarked or that match their profile, with email and browser push notifications.

## Overview
Users can see all their relevant grant deadlines in one calendar view and set 
reminders. Inspired by Instrumentl's grant calendar and GrantHub.

## Backend

### New notification preferences schema

Add to schema.ts:
```typescript
export const notificationPreferences = pgTable('notification_preferences', {
  id: uuid('id').primaryKey().defaultRandom(),
  userId: varchar('user_id').notNull().unique(),
  emailEnabled: boolean('email_enabled').default(true),
  pushEnabled: boolean('push_enabled').default(false),
  reminderDaysBefore: integer('reminder_days_before').array().default([30, 14, 7, 1]),
  // 30 days, 14 days, 1 week, 1 day before deadline
  digestFrequency: varchar('digest_frequency').default('weekly'), // daily | weekly | never
  pushSubscription: jsonb('push_subscription'), // Web Push subscription object
  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
});
```

### New API endpoints

**GET /api/calendar/events**
Returns calendar events for the logged-in user.

Query params:
- `start`: ISO date string (default: today)
- `end`: ISO date string (default: today + 3 months)
- `sources`: 'bookmarks' | 'matches' | 'all' (default: 'all')

Response:
```json
{
  "events": [
    {
      "id": "grant-uuid",
      "title": "Vinnova: Innovationsprojekt i företag",
      "deadline": "2026-03-15T23:59:00Z",
      "daysUntil": 24,
      "urgency": "medium",    // urgent (<7 days) | medium (7-30) | upcoming (>30)
      "source": "Vinnova",
      "amount": "500000",
      "matchScore": 87,
      "isBookmarked": true,
      "url": "https://vinnova.se/..."
    }
  ],
  "summary": {
    "urgent": 2,
    "thisMonth": 5,
    "nextMonth": 8
  }
}
```

**PUT /api/notifications/preferences**
Update notification preferences.

**POST /api/notifications/push-subscribe**
Register a browser push subscription.

Body: `{ subscription: PushSubscription }` (Web Push API object)

**DELETE /api/notifications/push-subscribe**
Unsubscribe from push notifications.

### Scheduled notification job

Create `server/jobs/deadlineNotifier.ts`:
```typescript
import { CronJob } from 'cron';

// Run daily at 08:00 Stockholm time
export const deadlineNotifierJob = new CronJob(
  '0 8 * * *',
  async () => {
    const users = await getUsersWithNotificationsEnabled();
    
    for (const user of users) {
      const prefs = user.notificationPreferences;
      const grants = await getGrantsForUser(user.id); // bookmarks + top matches
      
      for (const grant of grants) {
        if (!grant.deadline) continue;
        
        const daysUntil = getDaysUntil(grant.deadline);
        
        // Check if today matches any of user's reminder thresholds
        if (prefs.reminderDaysBefore.includes(daysUntil)) {
          if (prefs.emailEnabled) {
            await sendDeadlineEmail(user, grant, daysUntil);
          }
          if (prefs.pushEnabled && prefs.pushSubscription) {
            await sendPushNotification(user.pushSubscription, {
              title: `Deadline: ${grant.title}`,
              body: `${daysUntil} day${daysUntil !== 1 ? 's' : ''} left to apply`,
              url: `/grants/${grant.id}`,
              icon: '/favicon.ico'
            });
          }
        }
      }
      
      // Weekly digest
      if (prefs.digestFrequency === 'weekly' && isMonday()) {
        const upcomingDeadlines = grants
          .filter(g => g.deadline && getDaysUntil(g.deadline) <= 30)
          .sort((a, b) => new Date(a.deadline).getTime() - new Date(b.deadline).getTime());
        
        if (upcomingDeadlines.length > 0) {
          await sendWeeklyDigestEmail(user, upcomingDeadlines);
        }
      }
    }
  },
  null,
  true,
  'Europe/Stockholm'
);
```

### Email templates

Create `server/emails/deadlineReminder.tsx` using React Email or simple HTML:
Subject: ⏰ {daysUntil} dagar kvar — {grantTitle}

Hi {firstName},

Du har {daysUntil} dagar kvar att söka:

📋 {grantTitle}
💰 Upp till {maxAmount} SEK
🏢 {source}
📅 Deadline: {deadline}
🎯 Match: {matchScore}%

[Öppna ansökan →] [Skriv ansökan med AI →]

--
GetGrant.ai — Hitta och sök rätt bidrag
[Avsluta påminnelser]


## Frontend: Calendar page

Create `client/src/pages/calendar.tsx`:

### Calendar view (top half of page)

Use `react-big-calendar` (install: `npm install react-big-calendar date-fns`):
```tsx
import { Calendar, dateFnsLocalizer } from 'react-big-calendar';
import 'react-big-calendar/lib/css/react-big-calendar.css';

// Configure calendar with Swedish locale
// Color-code events by urgency:
// 🔴 Red: < 7 days
// 🟡 Yellow: 7-30 days  
// 🟢 Green: > 30 days
// 🔵 Blue: bookmarked grants
```

### List view (bottom half / toggle)

Toggle between calendar and list view:
UPCOMING DEADLINES

🔴 URGENT (next 7 days)
• Energimyndigheten: Gröna innovationer  →  3 days left  [Apply with AI]

🟡 THIS MONTH
• Vinnova: Innovationsprojekt → Feb 28 [Apply with AI] • Tillväxtverket: Regional tillväxt → Mar 5 [Apply with AI]

🟢 NEXT MONTH
• EU Horizon: EIC Accelerator  →  Apr 3  [Apply with AI]
...


### Notification settings panel (sidebar or modal)
📬 Notification Settings

Email reminders: [✅] 30 days before deadline [✅] 14 days before deadline
[✅] 7 days before deadline [✅] 1 day before deadline

Weekly digest: [✅ Every Monday]

Browser notifications:
[Enable browser notifications]  ← triggers Web Push permission request

[Save settings]


### Add to calendar button

On each grant deadline, show "Add to calendar" with:
- Google Calendar link (generate URL with event details)
- `.ics` file download (for Apple Calendar, Outlook)

Generate `.ics` format:
BEGIN:VCALENDAR VERSION:2.0 BEGIN:VEVENT DTSTART:20260315T235900Z DTEND:20260316T000000Z SUMMARY:Deadline: Vinnova Innovationsprojekt DESCRIPTION:Apply at https://getgrant.ai/grants/{id} URL:https://getgrant.ai/grants/{id} END:VEVENT END:VCALENDAR


## Navigation

Add calendar icon to main navigation (between "Grants" and "Profile"):
- Show badge with count of urgent deadlines (red dot with number if >0)

## Testing checklist
- [ ] Calendar loads and shows events from bookmarks
- [ ] Color coding works (red/yellow/green by urgency)
- [ ] List view toggle works
- [ ] Add to Google Calendar link opens correctly
- [ ] .ics download works and imports into calendar apps
- [ ] Email preferences save correctly
- [ ] Test email sends when deadline is X days away (use seeded test data)
- [ ] Push notification permission request appears
- [ ] Push notification received in browser
- [ ] Weekly digest email sends on Monday (test with manual trigger endpoint)
- [ ] Notification badge shows in nav when urgent deadlines exist
- [ ] Mobile calendar view is usable (consider switching to list-only on mobile)
Vill du att jag fortsätter med de resterande 4 funktionerna (Team Collaboration, Compliance Check, Smart Content Library, Post-award)?

ja

19:04
🟡 MEDIUM 1: Team Collaboration
Add team collaboration features so multiple people at a company can work together 
on grant applications — invite colleagues, see who's editing, leave comments, 
and track changes.

## Overview
Companies rarely apply for grants alone — a CEO writes the vision, a CFO handles 
the budget section, a technical lead writes the project description. This feature 
enables that workflow directly in GetGrant.ai, inspired by GrantPilot.se's 
collaboration features and Cogrant's team workspace.

## Database schema changes

Add to schema.ts:
```typescript
// Team members with roles
export const applicationCollaborators = pgTable('application_collaborators', {
  id: uuid('id').primaryKey().defaultRandom(),
  applicationId: uuid('application_id').notNull().references(() => applications.id),
  userId: varchar('user_id').notNull(),
  email: varchar('email').notNull(),
  role: varchar('role').notNull().default('editor'), // owner | editor | viewer | commenter
  invitedBy: varchar('invited_by').notNull(),
  status: varchar('status').default('pending'),      // pending | accepted | declined
  inviteToken: varchar('invite_token'),               // for email invite link
  inviteExpiresAt: timestamp('invite_expires_at'),
  joinedAt: timestamp('joined_at'),
  createdAt: timestamp('created_at').defaultNow(),
});

// Section-level comments
export const applicationComments = pgTable('application_comments', {
  id: uuid('id').primaryKey().defaultRandom(),
  applicationId: uuid('application_id').notNull().references(() => applications.id),
  sectionKey: varchar('section_key'),                 // null = general comment
  userId: varchar('user_id').notNull(),
  authorName: varchar('author_name').notNull(),
  authorEmail: varchar('author_email').notNull(),
  content: text('content').notNull(),
  resolved: boolean('resolved').default(false),
  resolvedBy: varchar('resolved_by'),
  resolvedAt: timestamp('resolved_at'),
  parentId: uuid('parent_id'),                        // for comment threads
  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
});

// Edit history per section
export const applicationSectionHistory = pgTable('application_section_history', {
  id: uuid('id').primaryKey().defaultRandom(),
  applicationId: uuid('application_id').notNull(),
  sectionKey: varchar('section_key').notNull(),
  content: text('content').notNull(),
  editedBy: varchar('edited_by').notNull(),
  editorName: varchar('editor_name').notNull(),
  wordCount: integer('word_count'),
  createdAt: timestamp('created_at').defaultNow(),
});

// Real-time presence tracking
export const applicationPresence = pgTable('application_presence', {
  id: uuid('id').primaryKey().defaultRandom(),
  applicationId: uuid('application_id').notNull(),
  userId: varchar('user_id').notNull(),
  userName: varchar('user_name').notNull(),
  userColor: varchar('user_color').notNull(),         // assigned color for cursor
  currentSection: varchar('current_section'),         // which section they're viewing
  lastSeenAt: timestamp('last_seen_at').defaultNow(),
});
```

## Backend: New API endpoints

Create `server/routes/collaboration.ts`:

### Invite management
POST /api/applications/:id/collaborators/invite Body: { email: string, role: 'editor' | 'viewer' | 'commenter' }

Generate secure invite token (crypto.randomUUID())
Store in application_collaborators with status: 'pending'
Send invite email via Resend
Response: { inviteId, email, role, expiresAt }
GET /api/applications/:id/collaborators

List all collaborators with their roles and status
Response: [{ userId, email, name, role, status, joinedAt }]
PUT /api/applications/:id/collaborators/:userId/role Body: { role: 'editor' | 'viewer' | 'commenter' }

Only application owner can change roles
DELETE /api/applications/:id/collaborators/:userId

Remove a collaborator (owner only, or self-removal)
GET /api/invites/:token/accept

Accept an invite link (user clicks link in email)
If user not logged in: redirect to login with returnUrl
If user logged in: accept invite, redirect to application
Update status to 'accepted', set joinedAt

### Comments
GET /api/applications/:id/comments Query: ?sectionKey=project_description (optional filter) Response: comments with author info, nested threads, resolved status

POST /api/applications/:id/comments Body: { sectionKey?: string, content: string, parentId?: string } Response: created comment with author info

PUT /api/comments/:commentId/resolve

Mark comment as resolved (only editor/owner)
DELETE /api/comments/:commentId

Delete own comment only

### Section history
GET /api/applications/:id/sections/:sectionKey/history Response: last 20 versions with editor name and timestamp

POST /api/applications/:id/sections/:sectionKey/restore/:historyId

Restore a previous version (saves current as new history entry first)

### Real-time presence (WebSocket)

Use the existing WebSocket setup if available, otherwise add:
```typescript
// server/websocket/collaboration.ts
import { WebSocketServer } from 'ws';

const rooms = new Map<string, Set<WebSocket>>();

// Events to handle:
// client → server: { type: 'join', applicationId, userId, userName }
// client → server: { type: 'section_focus', sectionKey }
// client → server: { type: 'typing', sectionKey, cursorPosition }
// client → server: { type: 'leave' }

// server → client: { type: 'presence_update', users: PresenceUser[] }
// server → client: { type: 'section_updated', sectionKey, editedBy, updatedAt }
// server → client: { type: 'comment_added', comment }

export function setupCollaborationWS(wss: WebSocketServer) {
  wss.on('connection', (ws, req) => {
    let applicationId: string;
    let userId: string;
    
    ws.on('message', (data) => {
      const event = JSON.parse(data.toString());
      
      if (event.type === 'join') {
        applicationId = event.applicationId;
        userId = event.userId;
        
        // Add to room
        if (!rooms.has(applicationId)) rooms.set(applicationId, new Set());
        rooms.get(applicationId)!.add(ws);
        
        // Broadcast presence update to all in room
        broadcastToRoom(applicationId, {
          type: 'presence_update',
          users: getPresenceUsers(applicationId)
        });
      }
      
      if (event.type === 'section_focus') {
        updatePresence(applicationId, userId, event.sectionKey);
        broadcastToRoom(applicationId, {
          type: 'presence_update',
          users: getPresenceUsers(applicationId)
        }, ws); // exclude sender
      }
    });
    
    ws.on('close', () => {
      if (applicationId) {
        rooms.get(applicationId)?.delete(ws);
        broadcastToRoom(applicationId, {
          type: 'presence_update',
          users: getPresenceUsers(applicationId)
        });
      }
    });
  });
}
```

## Permission middleware
```typescript
// server/middleware/collaborationAuth.ts
export async function requireApplicationAccess(
  req: Request, res: Response, next: NextFunction,
  requiredRole: 'owner' | 'editor' | 'viewer' | 'commenter' = 'viewer'
) {
  const { id: applicationId } = req.params;
  const userId = req.user?.id;
  
  const collaborator = await db.query.applicationCollaborators.findFirst({
    where: and(
      eq(applicationCollaborators.applicationId, applicationId),
      eq(applicationCollaborators.userId, userId),
      eq(applicationCollaborators.status, 'accepted')
    )
  });
  
  // Check owner access
  const application = await db.query.applications.findFirst({
    where: eq(applications.id, applicationId)
  });
  
  if (application?.userId === userId) return next(); // owner always has access
  if (!collaborator) return res.status(403).json({ error: 'Access denied' });
  
  const roleHierarchy = { viewer: 0, commenter: 1, editor: 2, owner: 3 };
  if (roleHierarchy[collaborator.role] < roleHierarchy[requiredRole]) {
    return res.status(403).json({ error: 'Insufficient permissions' });
  }
  
  next();
}
```

## Invite email template

`server/emails/collaborationInvite.tsx`:
Subject: {inviterName} har bjudit in dig till en bidragsansökan på GetGrant.ai

Hej,

{inviterName} vill samarbeta med dig på ansökan till:
📋 {grantTitle}
💰 Upp till {grantAmount} SEK
📅 Deadline: {grantDeadline}

Du har bjudits in som {role} och kan {roleDescription}.

[Öppna ansökan →]

Länken är giltig i 7 dagar.


## Frontend: Collaboration UI

### Invite panel (in application editor)

Add "Share" button to application header:
```tsx
// Clicking "Share" opens a modal:
<CollaborationModal applicationId={id}>
  <h3>Share application</h3>
  
  {/* Invite by email */}
  <div className="invite-form">
    <input placeholder="Colleague's email address" />
    <select>
      <option value="editor">Can edit</option>
      <option value="commenter">Can comment</option>
      <option value="viewer">Can view</option>
    </select>
    <button>Send invite</button>
  </div>
  
  {/* Current collaborators list */}
  <div className="collaborators-list">
    {collaborators.map(c => (
      <div key={c.userId} className="collaborator-row">
        <Avatar name={c.name} color={c.color} />
        <span>{c.email}</span>
        <span className="role-badge">{c.role}</span>
        {c.status === 'pending' && <span className="pending-badge">Pending</span>}
        {isOwner && (
          <RoleDropdown 
            currentRole={c.role} 
            onChange={(role) => updateRole(c.userId, role)} 
          />
        )}
      </div>
    ))}
  </div>
  
  {/* Share link */}
  <div className="share-link">
    <input value={shareUrl} readOnly />
    <button onClick={copyLink}>Copy link</button>
  </div>
</CollaborationModal>
```

### Live presence indicators

In the application editor header, show who else is currently viewing:
```tsx
<div className="presence-bar">
  {activeUsers.map(user => (
    <div 
      key={user.userId}
      className="presence-avatar"
      style={{ borderColor: user.color }}
      title={`${user.name} is viewing${user.currentSection ? ` — ${user.currentSection}` : ''}`}
    >
      {user.name.charAt(0).toUpperCase()}
    </div>
  ))}
  {activeUsers.length > 0 && (
    <span className="presence-text">
      {activeUsers.length} other{activeUsers.length !== 1 ? 's' : ''} viewing
    </span>
  )}
</div>
```

Color each user consistently using a fixed palette:
```typescript
const PRESENCE_COLORS = [
  '#3B82F6', '#10B981', '#F59E0B', '#EF4444', 
  '#8B5CF6', '#EC4899', '#06B6D4', '#84CC16'
];
// Assign based on: PRESENCE_COLORS[collaboratorIndex % PRESENCE_COLORS.length]
```

Show colored left border on the section another user is currently in:
```tsx
<div 
  className="section-card"
  style={{ 
    borderLeft: sectionActiveUser 
      ? `4px solid ${sectionActiveUser.color}` 
      : undefined 
  }}
>
  {sectionActiveUser && (
    <div className="section-active-indicator" style={{ color: sectionActiveUser.color }}>
      {sectionActiveUser.name} is editing this section
    </div>
  )}
  {/* section content */}
</div>
```

### Comments panel

Each section has a comment icon showing unresolved comment count:
```tsx
<div className="section-header">
  <h4>{section.title}</h4>
  <button 
    onClick={() => setActiveCommentSection(section.key)}
    className="comment-trigger"
  >
    <MessageSquare size={16} />
    {unresolvedCount > 0 && (
      <span className="comment-badge">{unresolvedCount}</span>
    )}
  </button>
</div>

{/* Comment panel slides in from right when active */}
<CommentPanel sectionKey={section.key} applicationId={id}>
  {comments.map(comment => (
    <CommentThread
      key={comment.id}
      comment={comment}
      onResolve={() => resolveComment(comment.id)}
      onReply={(content) => addComment({ 
        sectionKey: section.key, 
        content, 
        parentId: comment.id 
      })}
    />
  ))}
  <CommentInput 
    onSubmit={(content) => addComment({ sectionKey: section.key, content })}
    placeholder="Add a comment on this section..."
  />
</CommentPanel>
```

### Section history drawer

"Version history" button per section opens a side drawer:
Section History — Project Description

● Today 14:32 — Anna Lindström (you) [Restore] "Changed approach from B2B to B2B2C..."

● Yesterday 09:15 — Erik Johansson "Added market size data" [Restore]

● Feb 17 16:44 — Anna Lindström "Initial AI generation" [Restore]


## Role permissions matrix

| Action | Viewer | Commenter | Editor | Owner |
|--------|--------|-----------|--------|-------|
| View application | ✅ | ✅ | ✅ | ✅ |
| Add comments | ❌ | ✅ | ✅ | ✅ |
| Edit sections | ❌ | ❌ | ✅ | ✅ |
| Regenerate with AI | ❌ | ❌ | ✅ | ✅ |
| Invite collaborators | ❌ | ❌ | ❌ | ✅ |
| Change roles | ❌ | ❌ | ❌ | ✅ |
| Delete application | ❌ | ❌ | ❌ | ✅ |
| Export (PDF/Word) | ✅ | ✅ | ✅ | ✅ |

## Testing checklist
- [ ] Owner can invite colleague by email — invite email arrives
- [ ] Invite link works and logs in new user correctly
- [ ] Editor can edit sections, viewer cannot (403 returned)
- [ ] Two users editing same application — both see each other's avatar
- [ ] Section shows colored border when another user is in it
- [ ] Comment added by one user appears for other user without refresh
- [ ] Comment resolve works and comment collapses
- [ ] Version history shows last 20 edits with correct author names
- [ ] Restore previous version saves current version first
- [ ] Remove collaborator works — they lose access immediately
- [ ] Pending invite shows in collaborator list until accepted
- [ ] Mobile: collaboration panel collapses gracefully